## Research Depth & Scoring Accuracy — Open Items

Baseline: ~18 API calls per analysis run.

| ID | Type | Problem | Solution | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|--------|-------------|---------|----------|
| RA-07 | fix | `absorbLowConfidenceMeta` also calls `absorbAnalystMeta`, which can double-count targeted web calls into both `webSearchCalls` and `lowConfidenceTargetedWebSearchCalls`. | Remove `absorbAnalystMeta(analysisMeta, meta)` from `absorbLowConfidenceMeta`; track targeted-cycle usage only in targeted fields. | Accurate cost/usage accounting for targeted cycles. | 0 | 0% | P2 |
| RA-08 | improvement | Rubric calibration block only exposes Score 5 and Score 1 anchors, so models interpolate middle thresholds inconsistently. | Extend calibration/reminder blocks to include all 5 anchors (5/4/3/2/1) and ensure all dimensions have Score 2/4 anchor text. | Better run-to-run scoring consistency. | 0 | 0% | P1 |
| RA-10 | improvement | Critic reviews each dimension independently and misses cross-dimension contradictions (for example high evidence + very low ROI). | Add a cross-dimension coherence check block in critic prompt with explicit contradiction rules to flag. | Fewer structurally inconsistent final scorecards. | 0 | 0% | P1 |
| RA-11 | improvement | Polarity for `regulatory`, `change_mgmt`, `build_vs_buy` is still occasionally inverted in model outputs. | Add explicit "do NOT invert polarity" warnings in Phase 1 scoring and Phase 3 prompts for those dimensions. | Fewer wrong-direction score updates. | 0 | 0% | P1 |
| RA-12 | improvement | Weighted score ignores confidence; low-confidence 4/5 contributes equally to high-confidence 4/5. | Add confidence-weighted score alongside raw score (for example discounts: high 1.0, medium 0.85, low 0.65) and show both in UI. | Better ranking trust under uncertainty. | 0 | 0% | P1 |
| RA-13 | improvement | Low-confidence targeted cycle runs only once; if initial queries are weak, it stops without refinement. | Add one optional refinement pass when useful coverage is low (refined queries excluding failed patterns). | Better chance to recover weak dimensions. | 3-9 | 15-50% | P2 |
| RA-14 | improvement | Debate loop is reasoning-only; disputed claims are not explicitly fact-checked before final scoring. | Add a verification sub-phase between Critic and Phase 3 (`claimsToVerify` + live-search verifier prompt + verified status injection into Phase 3). | More evidence-grounded score changes in debate. | ~6 | ~33% | P2 |
| RA-15 | improvement | Phase 1 web pass is single-call across all dims, so low-weight dimensions can get poor search coverage. | Split Phase 1 web evidence into grouped passes and merge before scoring to guarantee minimum coverage across all dimensions. | More balanced evidence coverage by dimension. | +2 | ~11% | P2 |
| RA-16 | improvement | Discovery generation uses `SYS_ANALYST`; outputs can be generic vs strategic pivots. | Add `SYS_DISCOVER` system prompt for candidate generation; keep `SYS_ANALYST` for validation. | Better quality and specificity of discovery candidates. | 0 | 0% | P2 |
| FR-01 (In-App Cost Estimator) | feature | PMs cannot see expected/actual per-run analysis cost in-app. | Implement In-App Cost Estimator using `analysisMeta` + configurable pricing; show estimate vs actual and per-phase breakdown in UI and exports/debug logs. | Better budget control and model/mode decision-making. | 0 | 0% | P1 |
| FR-02 (Benchmark Regression Suite - Public Use Cases) | feature | Prompt/model changes can silently degrade scoring quality without a stable benchmark suite. | Add Benchmark Regression Suite with 10-20 public use cases, expected score/confidence ranges, and CI-friendly pass/fail drift checks. | Prevents quality regressions before release. | 0 | 0% | P1 |
