## Research Depth & Scoring Accuracy — Open Items

Baseline: ~18 API calls per analysis run.

| ID | Type | Problem | Solution | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|--------|-------------|---------|----------|
| RA-07 | fix | `absorbLowConfidenceMeta` also calls `absorbAnalystMeta`, which can double-count targeted web calls into both `webSearchCalls` and `lowConfidenceTargetedWebSearchCalls`. | Remove `absorbAnalystMeta(analysisMeta, meta)` from `absorbLowConfidenceMeta`; track targeted-cycle usage only in targeted fields. | Accurate cost/usage accounting for targeted cycles. | 0 | 0% | P2 |
| RA-08 | improvement | Rubric calibration block only exposes Score 5 and Score 1 anchors, so models interpolate middle thresholds inconsistently. | Extend calibration/reminder blocks to include all 5 anchors (5/4/3/2/1) and ensure all dimensions have Score 2/4 anchor text. | Better run-to-run scoring consistency. | 0 | 0% | P1 |
| RA-10 | improvement | Critic reviews each dimension independently and misses cross-dimension contradictions (for example high evidence + very low ROI). | Add a cross-dimension coherence check block in critic prompt with explicit contradiction rules to flag. | Fewer structurally inconsistent final scorecards. | 0 | 0% | P1 |
| RA-11 | improvement | Polarity for `regulatory`, `change_mgmt`, `build_vs_buy` is still occasionally inverted in model outputs. | Add explicit "do NOT invert polarity" warnings in Phase 1 scoring and Phase 3 prompts for those dimensions. | Fewer wrong-direction score updates. | 0 | 0% | P1 |
| RA-12 | improvement | Weighted score ignores confidence; low-confidence 4/5 contributes equally to high-confidence 4/5. | Add confidence-weighted score alongside raw score (for example discounts: high 1.0, medium 0.85, low 0.65) and show both in UI. | Better ranking trust under uncertainty. | 0 | 0% | P1 |
| RA-13 | improvement | Low-confidence targeted cycle runs only once; if initial queries are weak, it stops without refinement. | Add one optional refinement pass when useful coverage is low (refined queries excluding failed patterns). | Better chance to recover weak dimensions. | 3-9 | 15-50% | P2 |
| RA-14 | improvement | Debate loop is reasoning-only; disputed claims are not explicitly fact-checked before final scoring. | Add a verification sub-phase between Critic and Phase 3 (`claimsToVerify` + live-search verifier prompt + verified status injection into Phase 3). | More evidence-grounded score changes in debate. | ~6 | ~33% | P2 |
| RA-15 | improvement | Phase 1 web pass is single-call across all dims, so low-weight dimensions can get poor search coverage. | Split Phase 1 web evidence into grouped passes and merge before scoring to guarantee minimum coverage across all dimensions. | More balanced evidence coverage by dimension. | +2 | ~11% | P2 |
| RA-16 | improvement | Discovery generation uses `SYS_ANALYST`; outputs can be generic vs strategic pivots. | Add `SYS_DISCOVER` system prompt for candidate generation; keep `SYS_ANALYST` for validation. | Better quality and specificity of discovery candidates. | 0 | 0% | P2 |
| RA-17 | fix | Follow-up `challenge` and `question` intents (`useFollowUp.js` lines 283, 431) call `callAnalystAPI` with only 3 args — no web search capability. Only `re_search` has it (line 373-378). When a PM challenges a score, the analyst defends from model memory only, often citing stale or fabricated sources. | **Two-step adaptive approach.** Step 1 — add a `needsSearch` self-assessment to the follow-up prompt: after the analyst drafts its response, it must return an extra field `"searchNeeded": true/false` with `"searchReason": "<why context is sufficient OR what specific data gap requires search>"`. Step 2 — in `runIntentResponse`, make the first call without `liveSearch`. If `parsed.searchNeeded === true`, re-call with `{ liveSearch: true, includeMeta: true }` and a refined prompt that includes the identified gap: `"You identified this evidence gap: ${parsed.searchReason}. Now search for it."` This avoids unnecessary web calls when the analyst already has strong context (e.g. PM asks a clarification question about existing evidence) while enabling search when the analyst recognizes it's lacking data (e.g. PM cites a specific competitor the analyst has no info on). **Files:** `src/hooks/useFollowUp.js` lines 273-285 (question), 398-432 (challenge) in `runIntentResponse`. | Follow-ups get web search only when needed — smart cost/quality tradeoff. Analyst self-identifies gaps instead of always searching or never searching. | 0-1 per follow-up | ~0-5% | P0 |
| RA-20 | fix | Follow-up prompts in `useFollowUp.js` (`runIntentResponse`, lines 258-433) have zero source quality instructions. RA-09 source credibility rule only covers Phase 1. Follow-ups cite storefront homepages, vendor marketing pages, pricing comparison pages, and app marketplace listings as "evidence" — none containing verifiable claims or metrics. | **1. Prompt-level:** Add a `SOURCE QUALITY RULES` block to all 4 follow-up prompts that request sources (challenge, question, add_evidence, re_search): "UNACCEPTABLE sources: vendor product/pricing pages, storefront homepages, app marketplace listings, SEO landing pages, marketing brochures. These contain no verifiable claims. ACCEPTABLE sources: analyst reports, independent case studies, earnings calls, press coverage with named metrics, regulatory filings, peer-reviewed research, named customer testimonials with specific outcomes. If no acceptable source is available, state that explicitly — do not substitute a marketing page." **2. Template update:** Add `"sourceType":"<vendor\|press\|independent>"` to the source JSON template in all 4 handlers, matching the RA-09 Phase 1 schema. **Files:** `src/hooks/useFollowUp.js` lines 278-282 (question), 305-325 (add_evidence), 350-371 (re_search), 398-430 (challenge). | Follow-up sources are actual evidence, not marketing pages. e.g. analyst currently cites "Salsify.com" homepage → now must cite "Forrester Wave: PIM Q2 2025" or explicitly say no independent source found. | 0 | 0% | P0 |
| FR-01 (In-App Cost Estimator) | feature | PMs cannot see expected/actual per-run analysis cost in-app. | Implement In-App Cost Estimator using `analysisMeta` + configurable pricing; show estimate vs actual and per-phase breakdown in UI and exports/debug logs. | Better budget control and model/mode decision-making. | 0 | 0% | P1 |
| FR-02 (Benchmark Regression Suite - Public Use Cases) | feature | Prompt/model changes can silently degrade scoring quality without a stable benchmark suite. | Add Benchmark Regression Suite with 10-20 public use cases, expected score/confidence ranges, and CI-friendly pass/fail drift checks. | Prevents quality regressions before release. | 0 | 0% | P1 |

## Engine Extraction — Remaining Gaps

Residual items from the engine extraction plan (Steps 1–11 complete).

| ID | Type | Problem | Solution | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|--------|-------------|---------|----------|
| EE-01 | refactor | `engine/lib/researchBrief.js` has a hardcoded fallback map for 4 dimension IDs (`roi`, `evidence`, `build_vs_buy`, `change_mgmt`) in `whereToLookByDimension()`. Violates the engine's dimension-agnostic invariant. Config-first path exists but the fallback remains. | Complete `researchHints.whereToLook` and `queryTemplates` for all 11 default dimensions in `engine/configs/researchit-dimensions.js`, then delete the hardcoded map from `researchBrief.js`. Keep only a generic fallback for unconfigured dimensions. | Engine becomes fully dimension-agnostic as intended. | 0 | 0% | P1 |
| EE-02 | refactor | 7 files in `app/src/lib/` and 1 in `app/src/prompts/` are single-line re-export shims (`export * from "@researchit/engine"`), kept for backward-compat during migration. Adds indirection and obscures where logic lives. | Update all component/hook imports to use `@researchit/engine` directly, then delete the shim files (`arguments.js`, `confidence.js`, `rubric.js`, `dimensionView.js`, `json.js`, `followUpIntent.js`, `researchBrief.js`, `prompts/system.js`). | Cleaner import graph, smaller app surface. | 0 | 0% | P2 |
| EE-03 | cleanup | `ResearchIt—Engine-Extraction-Restructuring-Plan.md` at repo root is ~95% complete and references pre-migration paths. Suggests ongoing work that is largely finished. | Move to `docs/archive/` or delete. `docs/architecture.md` is now the authoritative structure reference. | Removes stale doc from repo root. | 0 | 0% | P2 |
