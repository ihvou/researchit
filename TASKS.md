## Open Items — Native Pipeline

Baseline (current observed range): ~18 API calls per scorecard run; ~10-20 calls per matrix run when low-confidence recovery is active.

Priority scale: P0 = reliability blocker / correctness bug, P1 = strong quality lift, P2 = strategic lift, P3 = deep/expensive optional mode.

Classic-flow target stack (quality-first default routing): OpenAI reasoning model as Analyst, Anthropic model as Critic, Gemini-grounded retrieval/query planning for web evidence, with fallback mesh when one provider degrades.

Execution order for user account roadmap: durable run persistence first, then auth, then billing gates, then collaboration (`DA-05 -> RU-03 -> UA-01/UA-02/UA-03 -> PAY-01/PAY-02/BIL-01 -> CO-01/CO-02`).

### Delivery Phases (Account -> Monetization/Sharing)

Phase 1 — account foundation (store researches under account, sign in/sign up):
- `DA-05` async orchestration + run persistence (Native + Deep Assist)
- `RU-01` client crash-safety restore
- `RU-03` run storage, retention, hard delete
- `UA-01` magic-link auth baseline
- `UA-02` anonymous-to-account claiming
- `UA-03` user research library + resume

Phase 2 — payment gate, payments/billing/balance, and sharing:
- `PAY-01` payment provider and checkout integration
- `PAY-02` balance ledger + usage charging + webhook reconciliation
- `BIL-01` free-run policy, balance gates, pre-run estimate gate, export gate
- `FR-01` in-app cost transparency
- `CO-01` share links/TTL/access controls
- `CO-02` collaborative comments and review threads

| ID | Type | Problem | Solution | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|--------|-------------|---------|----------|
| DA-02 (Deep Assist Quality Recovery Loop) | feature | Deep Assist merge can still leave cells/dimensions with low confidence, contradicting providers, or thin evidence — current flow accepts the merge as final. | After `mergeDeepAssistMatrix` / `mergeDeepAssistScorecardPayloads`, select recovery candidates: cells/dims where `providerAgreement === "contradict"` OR `confidence === "low"` OR `sources.length < 2`. Cap at `maxRecoveryCells` (default 6 matrix / 4 scorecard). For each candidate, reuse the existing targeted recovery flow (query plan → web search → rescore) already built for Native. In matrix: pipe candidates into `selectMatrixTargetedCells` with a `deepAssistRecovery` flag so diagnostics distinguish DA recovery from native targeted. In scorecard: pipe into `selectTargetedCycleDimensions`. Skip recovery if all providers agreed and confidence >= medium. Track `deepAssistRecoveryCandidates`, `deepAssistRecoveryUpgraded`, `deepAssistRecoveryValidatedLow` in `analysisMeta`. Budget: reuse existing `limits.targetedBudgetUnits` or add `limits.deepAssistRecoveryBudget`. Model: same retrieval capability as native targeted (Gemini-grounded). | Prevents false-complete outputs in hard domains. Reuses existing targeted infrastructure — no new actor needed. | +1-6 | +5-30% | P0 |
| DA-05 (Async Orchestration + Run Persistence, Native + Deep Assist) | feature | Closing/reloading browser tabs can drop in-flight progress visibility and make long runs feel lost. Deep Assist has higher risk due to multi-minute, multi-provider execution. | Introduce durable async run orchestration with persisted run state, resumable steps, cancellation, and idempotent retries for both Native and Deep Assist. Progress events must survive reconnects and recover by `runId`. | Foundation for reliable resume/reconnect behavior and prerequisite for account-backed run continuity. | 0 (quality infra) | 0% | P0 |
| RU-01 (Client Crash-Safety Draft Restore) | feature | Before full server-backed persistence, users can still lose visible in-progress context after accidental tab close or refresh. | Persist lightweight in-progress snapshots locally, restore with resume/clear prompt on reload, and show unload guard when a run is active. | Immediate UX safety net while durable orchestration is rolling out. | 0 | 0% | P1 |
| RU-03 (Run Storage, Retention, and Hard Delete) | feature | Account features require explicit lifecycle rules for run artifacts and user-controlled deletion. | Add durable run artifact storage model with retention metadata, hard-delete path, and compatibility with existing export/import artifacts. | Enables trustworthy persistence and privacy controls before broad auth rollout. | 0 | 0% | P1 |
| UA-01 (Magic-Link Auth Baseline) | feature | Returning users currently rely on local state and cannot reliably recover work across devices/sessions. | Implement email magic-link sign-in/up, session handling, and account shell integrated with persisted run retrieval. | Baseline identity layer for returning users and durable history access. | 0 | 0% | P1 |
| UA-02 (Anonymous-to-Account Claiming) | feature | Hard auth gate before users see value increases drop-off and can orphan anonymous runs. | Allow anonymous first run, then claim recent runs after sign-up using secure ownership handoff. | Keeps low-friction onboarding while preserving continuity after signup. | 0 | 0% | P1 |
| UA-03 (User Research Library + Resume) | feature | Returning users need a reliable home for owned runs, status, and continuation actions. | Add "My Researches" library with status filters (`running/completed/degraded/failed`), resume/open/delete/export actions, and reconnect by `runId`. | Makes Researchit usable as an ongoing workspace, not a single-session tool. | 0 | 0% | P1 |
| PAY-01 (Payments Provider Integration) | feature | Billing gates cannot work without a reliable top-up checkout path tied to account identity. | Integrate payment provider checkout/session return flow and account crediting entry point; support test/sandbox and production modes. | Enables monetization and funding of paid runs. | 0 | 0% | P1 |
| PAY-02 (Balance Ledger + Usage Charging + Webhook Reconciliation) | feature | Balance changes can drift without a canonical ledger and asynchronous payment reconciliation. | Implement append-only balance ledger, per-run debit records, idempotent webhook reconciliation, and audit trail for credits/debits/refunds/adjustments. | Accurate balances and trustworthy billing operations. | 0 | 0% | P1 |
| BIL-01 (Free Run + Balance Gate Policy) | feature | Onboarding/billing flow in product scenarios requires explicit gate rules tied to identity, payments, and persisted runs. | Add free-run entitlement policy, pre-run estimate checks, low-balance handling, export/top-up gates, and UX messaging wired to account state + ledger. Depends on `PAY-01` and `PAY-02`. | Aligns onboarding promise with operable monetization and cost controls. | 0 | 0% | P1 |
| CO-01 (Share Links, TTL, and Access Controls) | feature | Shared research is needed for async review, but current flow lacks controlled link-based access and expiry. | Implement share links for one or multiple researches, configurable TTL, revoke controls, and "shared with me" access model. | Enables lightweight collaboration without forcing immediate full account creation for reviewers. | 0 | 0% | P2 |
| CO-02 (Collaborative Comment Threads) | feature | Current challenge threads are single-user; reviewer feedback cannot be captured inline in shared research. | Add per-dimension/per-cell comments and replies with participant labeling, notifications, and owner moderation/revoke actions. | Turns outputs into collaborative decision artifacts instead of static reports. | 0 | 0% | P2 |
| FR-02 (Benchmark Regression Suite) | feature | Pipeline/prompt/model changes can silently degrade quality. No way to measure impact of improvements. | Build hybrid benchmark stack: (1) replay regression tests in CI using dumped model/tool responses (zero API spend), (2) small live canary subset on schedule to detect provider/retrieval drift, (3) full live benchmark pre-release and before major quality claims. Start with 6-8 gold cases, expand to 20. Define fixed scoring rubric + scorer protocol so DA-04 comparisons are reproducible. Prerequisite for DA-04. | Measurement infrastructure: prevents regressions and makes "beats mainstream deep research" claims auditable when DA-04 runs on top. | low recurring + periodic full run | low recurring + periodic full run | P3 |
| DA-04 (External Deep Research Head-to-Head Harness) | feature | "Beats mainstream deep research" cannot be claimed without direct side-by-side evidence. | Add benchmark harness that runs fixed prompts against Researchit, ChatGPT Deep Research, Claude Research, and Gemini Research; score with rubric (accuracy, source quality, decision utility, contradiction handling). Publish win/loss deltas in debug artifacts. Depends on FR-02 for infrastructure. | Objective proof that quality direction is working. | benchmark-only | benchmark-only | P3 |
| DA-06 (Deep Assist Run Manifest + Evidence Cache) | feature | Retries/re-runs can re-pay for identical deep calls and make audits hard. | Persist a deterministic run manifest (prompt hash + config version + provider/model set + timestamps) and cache raw provider outputs + normalized extraction. Reuse cache on safe retries/replays; expose provenance in debug/export artifacts. | Lower avoidable cost, better reproducibility, stronger auditability. | 0 (happy path) | 0% to lower | P1 |
| DA-07 (Provider Data Governance + Privacy Controls) | feature | Three-provider Deep Assist increases data handling risk (PII leakage, unclear retention, compliance ambiguity). | Add provider-level data governance controls: redaction policy before provider calls, provider allow/deny list per run, retention policy metadata, and explicit diagnostics about where data was sent. | Required trust/compliance foundation for enterprise usage. | 0 | 0% | P1 |
| ENG-01 (Extract Shared Engine Utilities) | refactor | ~300 lines of utility functions are duplicated between `analysis.js` and `matrix.js` (`normalizeEvidenceMode`, `markDegraded`, source verification, fuzzy matching, URL normalization, etc.) with divergent naming (`cleanString` vs `cleanText`). Bug fixes in one file won't propagate to the other. | Extract shared functions into dedicated `engine/lib/` modules (e.g. `engine/lib/evidenceMode.js`, `engine/lib/sourceVerification.js`, `engine/lib/degraded.js`). Both pipelines import from shared modules. | Eliminates divergence risk and reduces engine code by ~300 lines. | 0 | 0% | P1 |
| ENG-02 (Eliminate ACTIVE_RUNTIME Global State) | refactor | Scorecard pipeline uses a mutable global `ACTIVE_RUNTIME` that many internal functions read via `getRuntime()`. Not safe for concurrent runs in the same process — will break when DA-05 (async orchestration) lands. Matrix pipeline already passes config explicitly. | Refactor scorecard pipeline to pass config/transport explicitly like matrix does. Remove `ACTIVE_RUNTIME`, `getRuntime()`, and all implicit readers. | Prerequisite for DA-05 concurrent run safety. | 0 | 0% | P1 |
| ENG-03 (Unify Deep Assist Provider Options Resolution) | refactor | `resolveDeepAssistProviderRequestOptions` has two different signatures: analysis.js reads from `ACTIVE_RUNTIME`, matrix.js takes explicit `config`. Calling the wrong one silently returns wrong defaults. | Unify into a single shared function with explicit config parameter (same pattern as matrix.js). Depends on ENG-02. | Prevents silent misconfiguration in multi-provider flows. | 0 | 0% | P2 |
| ENG-04 (Source Verification Dedup Guard) | fix | Matrix native flow runs `verifyMatrixCellSources` up to 3 times (reconcile, post-reconcile, post-deep-assist). `sourceFetchCache` prevents redundant fetches but confidence penalty logic can stack on the same sources. | Add a per-cell verification-applied flag or track which cells have already been penalized to prevent double/triple confidence downgrades from the same evidence. | Prevents over-penalization of matrix cell confidence. | 0 | 0% | P3 |
| ENG-05 (Align analysisMeta Initialization) | refactor | `analysisMeta` is initialized in three places (App.jsx partial subset, engine `createInitialState` full set, `runMatrixAnalysis` partial re-spread) with different field sets. Stale values from one layer can leak through. | Centralize `analysisMeta` initialization in engine only. App.jsx should not pre-build `analysisMeta` — let the engine own the full shape. | Prevents subtle field mismatch bugs as diagnostics expand. | 0 | 0% | P3 |
| RQ-26 (Retrieval Mesh: Multi-Engine Search) | feature | Single search stack can miss important sources and collapse coverage quality. Overlaps with CF-08 (Gemini as default web evidence provider); this item covers fallback providers beyond Gemini. | Add pluggable retrieval mesh with Gemini-grounded retrieval as default engine plus fallback providers, dedupe/rank results by source quality/recency, expose per-provider hit diagnostics. | Higher recall, lower single-provider failure risk. | +6-18 | +30-90% | P2 |
| RQ-25 (Model Portfolio by Function) | feature | One-model-heavy pipelines can share blind spots across phases. Includes multi-model ensemble (Anthropic, Gemini adapters). | Route phases to role-specialized models with config overrides and fallbacks. Add multi-provider adapters. Routing infrastructure exists (`withRoleModelOptions`, per-role model config), adapters are missing. | Better robustness and lower correlated errors. Cross-provider disagreement exposes weak claims earlier. | ~same | ~0% | P2 |
| RQ-02 (Red Team) | feature | Critic audits existing claims but does not construct the strongest possible counter-case. Mainstream deep research sometimes naturally surfaces structural risks that our pipeline misses because every prompt asks "find evidence for X." | Add a single Red Team LLM call after the critic phase (scorecard: after phase 3 reconcile, before finalization; matrix: after critic flags are applied, before consistency/discovery). Prompt receives the full audited output and asks: "Construct the strongest case for why this conclusion is wrong — focus on structural market risks missed, failure modes of the proposed approach, adversary/incumbent responses not considered, and assumptions that could be catastrophically wrong." Output shape: `{ "redTeamVerdict": "<1-2 sentence overall>", "dimensions": { "<dimId>": { "threat": "<strongest counter-argument>", "missedRisk": "<risk not in analyst output>", "severityIfWrong": "high|medium|low" } } }`. For matrix: same shape but keyed by `subjectId::attributeId`. Red team findings get merged into `risks` field of each dim/cell and surfaced in a dedicated "Red Team" section in exports/UI. Does NOT change scores — only adds risk context. Model: use Critic model (Anthropic) — different "brain" from analyst. Add `redTeamEnabled` config flag (default true) and `analysisMeta.redTeamCallMade` / `analysisMeta.redTeamHighSeverityCount` diagnostics. | Improves risk realism. Single call, high value, no new infrastructure. | +1 (scorecard) / +1 (matrix) | +5-10% | P2 |
| RQ-05 (Counterfactual Evidence) | feature | Evidence collection prompts ask "find evidence about X for dimension Y" — this naturally over-indexes supporting evidence. Mainstream deep research sometimes surfaces disconfirming data because it explores broadly. Our structured pipeline doesn't. | NOT a new actor. Extend the existing Niche Query Strategist (RQ-15) output to include a `counterfactualQueries` field per dimension/cell alongside existing `querySeeds`. For each dimension/cell, generate 2-3 disconfirming queries: "why [subject] might fail at [dimension]", "[dimension] criticism OR failure cases for [subject]", "competitors outperforming [subject] on [dimension]." Feed counterfactual queries into the existing targeted recovery cycle — they run through the same search → harvest → rescore flow. In the rescore prompt, explicitly label evidence as `supporting` vs `counterfactual` so the model weighs it correctly. Counterfactual findings merge into existing `arguments.limiting` in scorecard dims and matrix cells. For scorecard: extend `buildNicheQueryStrategistPrompt` to request counterfactual seeds. For matrix: extend `buildMatrixQueryStrategistPrompt` similarly. Track `counterfactualQueriesGenerated` and `counterfactualFindingsUsed` in `analysisMeta`. Config: `limits.counterfactualQueriesPerDim` (default 2). Model: same retrieval capability as native targeted (Gemini-grounded). No new LLM role. | Reduces confirmation bias using existing infrastructure. Cheap — just smarter query templates. | +2-4 per targeted dim/cell | +10-20% | P2 |
| RQ-19 (Evidence Graph Layer) | feature | Claims are stored as prose; provenance and contradiction tracking are hard. | Introduce structured claim graph (`claim → evidence nodes → source spans`) with recency/verification/conflict metadata. Score from graph signals, not only narrative. | Strong auditability and better machine-checkable logic. | 0-2 | 0-10% | P2 |
| RQ-24 (Research Memory with Revalidation) | feature | Useful prior evidence is not reused systematically; stale reuse risk is unmanaged. | Add memory store of vetted claims with decay + mandatory revalidation before reuse in new runs. | Faster future runs with controlled freshness risk. | 0-2 | 0-10% | P2 |
| FR-01 (In-App Cost Transparency) | feature | No-cap Deep Assist policy needs clear visibility into expected and actual cost. | Show estimated range before launch, live spend/progress during run, and actual by phase/provider in exports/debug logs. | Preserves quality-first policy while avoiding surprise spend. | 0 | 0% | P1 |
| RA-12 | improvement | Weighted score ignores confidence quality. | Add confidence-weighted score alongside raw score (e.g. H=1.0, M=0.85, L=0.65) and display both. Depends on RQ-21. | Better ranking trust under uncertainty. | 0 | 0% | P2 |
| RA-16 | improvement | Discovery generation quality can be generic. | Add dedicated `SYS_DISCOVER` prompt optimized for strategic adjacent opportunities and clearer candidate rationale. | Better discover candidates. | 0 | 0% | P2 |
| RQ-09 (Synthesizer Actor) | feature | Currently the analyst writes both evidence and summary — same model, same biases. Executive synthesis (MX-07, crossMatrixSummary, scorecard summary) is generated by the same actor that collected the evidence, creating self-reinforcing narratives. | Add a dedicated synthesizer LLM call as the final pipeline step, AFTER all scoring/verification/red-team is done. Synthesizer receives: (1) the full audited dim/cell scores with confidence, (2) red team findings if RQ-02 is enabled, (3) decision context from UX-01, (4) provider agreement signals if Deep Assist. Synthesizer does NOT see raw evidence prose — only structured scores, confidence, risks, and limiting arguments. This forces it to synthesize from signals, not parrot analyst wording. Output shape for scorecard: `{ "executiveSummary": "<3-5 sentences>", "decisionImplication": "<what this means for the decision>", "keyUncertainties": ["<uncertainty 1>"], "dissent": "<strongest case against the conclusion>" }`. For matrix: replaces current `executiveSummary` generation (decisionAnswer, closestThreats, whitespace, keyRisks, etc.) — same fields, different author. Model: use a DIFFERENT model from analyst. Recommended: Anthropic (if analyst is OpenAI) or OpenAI (if analyst is Anthropic). The point is model diversity for the narrative layer. Config: `models.synthesizer` with same provider/model/baseUrl shape as other roles. Fallback to critic model if not configured. Add `analysisMeta.synthesizerModel` and `analysisMeta.synthesizerCallMade`. | More neutral summaries. Single call. Forces narrative independence from evidence collection. | +1 | +5% | P2 |
| CF-02 | improvement | Many config dimensions still lack robust polarity/research hints. | Add `polarityHint` and rich `researchHints` (`whereToLook`, `queryTemplates`) to all dimensions across configs. Prerequisite: ensure engine retrieval actually consumes these hints. | Better targeted retrieval for every research type. | 0 | 0% | P2 |
| DM-01 (Demo Research Pack) | feature | New users start with empty state and cannot benchmark output shape quickly. | Ship static demo pack fixtures (scorecard + matrix) and tag as demo; include representative high-quality examples. | Faster onboarding and quality expectations calibration. | 0 runtime | 0% | P2 |
| CF-06 | improvement | Config definitions remain harder to contribute to than necessary. | Refactor config schema to self-contained per-config objects + shared global defaults with lightweight normalization. | Easier contribution and lower config drift risk. | 0 | 0% | P2 |
| CF-03 | fix | Some scorecard configs can still rely on default output mode behavior. | Keep explicit `outputMode` in every config to avoid implicit fallback bugs. | Stability and clarity. | 0 | 0% | P2 |
| UX-02 (Source Taxonomy and Transparency) | feature | Users cannot see which sources were used, corroborating, unverified, or excluded — and why. The engine already tracks `verificationStatus` (`verified_in_page`, `not_found_in_page`, `fetch_failed`, `invalid_url`, `name_only_in_page`) and `sourceType` (`vendor`, `press`, `independent`) plus stale-evidence ratios — but the UI shows sources as a flat list with no status indicators. | **Engine changes:** Add a computed `displayStatus` field to each source in `normalizeSourceList` output, mapping existing signals to user-facing categories: `cited` (verificationStatus=verified_in_page), `corroborating` (verificationStatus=name_only_in_page AND sourceType != vendor), `unverified` (verificationStatus=not_found_in_page OR fetch_failed), `excluded_marketing` (sourceType=vendor AND verificationStatus != verified_in_page), `excluded_stale` (staleEvidenceRatio >= 0.6 for sources with only pre-cutoff year references). Add `sourceUniverse` summary to analysisMeta: `{ cited: N, corroborating: N, unverified: N, excludedMarketing: N, excludedStale: N, total: N }`. **UI changes (scorecard):** In ExpandedRow source list, add colored status pill next to each source (green=cited, blue=corroborating, yellow=unverified, red=excluded). Add collapsible "Source Universe" summary bar above source list showing counts by status. **UI changes (matrix):** In cell detail/debate view, same source pills. In MatrixSummaryTab, add aggregate source quality stats. **Export changes:** Include `displayStatus` in JSON exports. In HTML/PDF exports, render source status inline. Applies to both Native and Deep Assist — same source fields are populated in both flows. | Full evidence transparency. Makes our verification advantage visible to users. Zero additional API calls. | 0 | 0% | P1 |
| RQ-07 (Deep Mode: Triple Baseline) | feature | Standard mode lacks explicit model-divergence signal. | Add optional deep mode with parallel baseline passes and disagreement-guided retrieval focus. Depends on RQ-25. | Better calibration on high-stakes runs. | +3 (parallel) | +15% | P3 |
| RQ-08 (Targeted Multi-Model Search) | feature | Single-model query framing misses retrieval diversity in disputed cells. | In deep mode, run disputed-dimension retrieval through multiple models, merge evidence, and reconcile. Depends on RQ-25 + RQ-07. | Higher recall in contentious cells. | +4-8 | +20-40% | P3 |

### Removed / Merged Items

| Original ID | Action | Reason |
|-------------|--------|--------|
| RA-15 (Grouped Passes) | Merged into RQ-11 | Same problem (uneven coverage) from different angle. RA-15 description already acknowledged "tie into round-robin planner". |
| RQ-01 (Fact-Checker) | Merged into RQ-20 | RQ-01 description already said "implement on top of RQ-20 verifier core (no duplicate verifier logic)". Not a separate backlog item. |
| RQ-03 (Multi-Model Ensemble) | Merged into RQ-25 | Same feature (multi-provider phase routing) from different framing. Routing infrastructure already exists. |
| RQ-16 (Domain Adapters) | Removed | Cannot direct web_search to specific sources — it is a black box. Curated source packs per industry require actual API integrations (PubMed, SEC EDGAR, etc.) that do not fit current architecture. Domain-specific retrieval belongs in Deep Assist mode. |
| RA-14 (Debate Loop Verification) | Merged into RQ-20 | Already marked as implementation dependency of RQ-20 in previous version. |

---

## Deep Assist — see `docs/spec-deep-assist.md`

Deep Assist introduces an alternative evidence collection layer that uses external deep research APIs (Claude, ChatGPT, Gemini) instead of internal web_search-based retrieval. The critic, scoring, verification, and synthesis layers remain shared with the Native pipeline.

User selects evidence mode at research launch time — not in config. Same config works with either mode. Deep Assist default is 3 providers (quality-first) with no hard spend cap, but with strict retry/timeout safeguards and full cost transparency.
