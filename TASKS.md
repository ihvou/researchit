## Open Items — Canonical Pipeline

Current baseline: ~18 API calls per scorecard run; ~10-20 calls per matrix run when low-confidence recovery is active.

Priority scale:
- **P0** — reliability blocker / correctness bug / prerequisite before the next quality revalidation.
- **P1** — strong quality lift, or core product surface that unlocks user value (auth, billing, run library).
- **P2** — strategic lift; meaningful but not blocking.
- **P3** — deep/expensive optional modes, refactors, or long-term architectural shifts.

---

### Current Execution Order

**Tier 0 (done).** Infrastructure prerequisites shipped: abort-source attribution (INFRA-01), finish-reason + truncation diagnostics (INFRA-02), chunk lifecycle events + stable cell-count-based chunking (INFRA-03, CHUNK-01), KV stage-cache + resume UI + cache diagnostics (RES-01a/b/c), stage-failure cause classification (QG-06), parse-failure guardrails (QG-03), and reconcile acceptance gating (QG-04). These closed the forensic gap: root-cause diagnosis of the 2026-04-20T18 integration run took minutes rather than hours. One original Tier 0 epic remains: DA-05 (async orchestration + persistence).

**Tier 1 (implemented; pending re-validation).** SQ-08..SQ-14 — numeric-confidence coercion, Gemini URL canonicalization, grounded-metadata plumbing, cross-check cited URLs vs grounded set, tiered verification outcomes, fabrication-ratio gate, confidence-merge preference. Shipped in commit `007882f`. Status in the 2026-04-20T18-23 run was masked by the regressions captured in Tier 1.5 below. Re-validate after Tier 1.5.

**Tier 1.5 (P0, new — from 2026-04-20T18-23-25-787Z.json integration run).** The run reached stage 15 and failed the decision gate with `fabricationRatio: 0.3125` driven by instrumentation bugs and silent compaction — not by actual evidence quality. New tasks: RTR-01 (repair Gemini grounding extractor), RTR-02 (force `google_search` tool use per chunk), VER-01 (re-verify post-recovery sources), DEF-01 (retry missing defend responses), SYN-01 (remove silent prompt compaction on stage 14), GATE-01 (`fabricationSignal: unknown` when grounded set absent), PLN-01 (planner unit-count assertion), PAR-01 (parallelize 03a/03b chunks — safe post-CHUNK-01), RES-02 (raw provider-response cache for 03a/03b/08), UI-01 (progress-tab blocked-state regression). Most Tier 1.5 fixes are now complete; remaining open P0s are RES-02 reprocess-stage flow and DA-05 async orchestration.

Side diagnostic (not a P0 task yet): the failing bundle shows `missReason: "cache_unavailable"` on stage-cache lookups. KV itself is healthy (run-list and output storage work); the failure is HTTP-level on the stage-cache API round-trip at [app/src/lib/stageCache.js:147](app/src/lib/stageCache.js:147). If this reappears in the next integration run, file a dedicated task.

**Tier 1.6 (P0, new — from 2026-04-21T12-46-27-314Z.json integration run).** Tier 1.5 landed successfully but exposed two new classes of bug downstream: (a) cell-level `groundedByProvider` metadata is not propagated from stage-level grounded set (277 grounded stage-level → `false` × 133 at cell-level); (b) the "fabricated" tier fires on "absent from grounded set" alone instead of the quality-bar three-criteria definition (grounded-set miss ∧ HEAD 404 ∧ content contradiction). Result: run `1776773751238-339gl1` (9×8 matrix, 26.17 min, $1.79) shipped with `fabricationRatio: 0.9124` driven by definitional over-flagging — a direct violation of the "Maximum Extraction, Not Minimum Filtering" principle. New tasks: **RETR-01** (promoted P1 → P0; retrieve-then-reason split closes the architectural root cause by construction), **GND-01** (stage→cell grounded-metadata propagation), **FAB-01** (fabricated-tier three-criteria definition), **GND-02** (stage 08 recovery fabrication audit — 237 of 244 flagged), **GATE-02** (decision-gate emission visibility — `fabricationSignal` token absent from entire bundle). RES-02 (Tier 1.5, still open) remains in scope because it unlocks cheap RETR-01 iteration. Recommended ship order: RES-02 → GATE-02 → RETR-01 ⫽ GND-01 + FAB-01 ⫽ GND-02 (parallel where code is independent).

**Tier 2 (P1, P2).** CR-01 (critic with web search) remains P2. RETR-01 promoted to P0 in Tier 1.6 above.

**Tier 3 (P3).** CONF-01 (deterministic confidence) unchanged; depends on RETR-01 + FR-02 benchmark.

**Delivery Phases (Account → Monetization/Sharing).** Phase 1 (account foundation): `DA-05 → RU-03 → UA-01, UA-02, UA-03, RU-01`. Phase 2 (payments + sharing): `PAY-01, PAY-02 → BIL-01, FR-01, FR-04 → CO-01 → CO-02`. Phases can overlap Tier 1.5/1/2/3 work but do not precede the Tier 1.5 batch.

**Classic-flow target stack** (quality-first default routing): OpenAI reasoning model as Analyst, Anthropic as Critic, Gemini-grounded retrieval for web evidence. Strict model pinning, fail-fast on provider/model errors, no silent failover.

---

### Task Backlog

| ID | Type | Problem | Solution | Acceptance | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|------------|--------|-------------|---------|----------|
| **RTR-01** (Repair Gemini grounding extractor) | fix | Debug bundle `analysis-debug-bundle-2026-04-20T18-23-25-787Z.json` stage 03b chunk `c02.a`: `webSearchCalls: 16, liveSearchUsed: true, groundedSourceCount: 0`. Gemini ran 16 searches, but `extractGeminiGroundingSources` in [app/api/providerCalls.js:203](app/api/providerCalls.js:203) returned zero grounded URLs despite `groundingMetadata` being present in the raw response. Downstream: every emitted URL tagged `groundedByProvider: false` → `fabricationSignal: high` → SQ-13 gate flags 55 of 176 sources (31%) as fabricated. Tier 1 instruments this wire; the wire itself is broken. | 1) In [app/api/providerCalls.js](app/api/providerCalls.js) `extractGeminiGroundingSources`, iterate `candidates[].groundingMetadata.groundingChunks[]` and read `.web.uri` + `.web.title`; also consume `groundingMetadata.groundingSupports[]` for claim-to-source anchors; fall back to `groundingMetadata.searchEntryPoint.renderedContent` link extraction if `groundingChunks` absent. 2) Canonicalize each URI via the SQ-09 canonicalizer before emitting (strip `vertexaisearch.cloud.google.com/grounding-api-redirect/...` wrappers → resolved final URL where possible). 3) Emit reason code `grounding_extraction_failed` on the call when `webSearchCalls > 0` AND extracted count is 0; log full `groundingMetadata` keys at debug level (key shape only, not values) so the next mismatch surfaces what changed. 4) Adapter unit test under `engine/__tests__/` (or next to providerCalls) with a stored fixture Gemini response containing 10 grounding chunks + 5 supports: asserts `meta.groundedSources.length === 10`, each with resolved URL + title. 5) Integration smoke: one 03b chunk in the re-run 9×8 matrix emits `groundedRatio >= 0.5` in `citations`. | Rerun of 9×8 healthcare matrix: every stage 03b chunk with `webSearchCalls > 0` also has `groundedSourceCount > 0`. Zero `grounding_extraction_failed` reason codes on successful Gemini runs. Fixture adapter test passes: 10 chunks in, 10 grounded sources out, URIs canonicalized. Stage 03b aggregate `groundedRatio >= 0.5` on the rerun (vs 0 today). | Restores the Tier 1 SQ-10/SQ-11 signal path. Without this, SQ-13 fabrication gate fires on instrumentation bugs not on actual fabrication. | 0 | 0% | Done |
| **RTR-02** (Force `google_search` tool use per 03b chunk) | fix | Same bundle, stage 03b chunk `c01`: `webSearchCalls: 0, liveSearchUsed: false, citations.totalUrls: 19, groundedUrls: 0`. Gemini skipped `google_search` entirely for that chunk yet still emitted 19 URLs — all ungrounded, all flagged `fabricationSignal: high`. Current Gemini payload does not pin `tool_config.function_calling_config.mode`, so the model decides on its own whether to invoke search. On "easy-looking" chunks it frequently declines, leaving Analyst to hallucinate URLs. | 1) Make the post-call signal authoritative: in [app/api/providerCalls.js](app/api/providerCalls.js), when `options.liveSearch === true` and observed `webSearchCalls === 0`, set `meta.noSearchPerformed: true` and emit reason code `stage_03b_no_search_performed` on the returned call. Acceptance and retries key off this observed value, not payload shape. 2) In [engine/pipeline/stages/03b-evidence-web.js](engine/pipeline/stages/03b-evidence-web.js) `gatherMatrixWeb`, when a chunk returns `meta.noSearchPerformed: true`, re-queue that chunk once with an explicit prompt hint "you must call `google_search` for each cell before proposing sources"; second miss bubbles as chunk failure → CHUNK-01 split path. 3) Keep payload forcing as a best-effort nudge: in Gemini request body, when `liveSearch === true`, include `tool_config: { function_calling_config: { mode: "ANY", allowed_function_names: ["google_search"] } }`. 4) Adapter unit test: Gemini request with `liveSearch: true` includes the `tool_config` payload fields. 5) Chunk-retry unit test: first call returns `webSearchCalls: 0` → one retry; second call with `webSearchCalls > 0` → success; persistent zero-search → chunk failure. | Rerun of 9×8 healthcare matrix: `webSearchCalls > 0` on every stage 03b chunk. Zero `stage_03b_no_search_performed` reason codes on the final successful rerun. Unit tests (adapter payload + chunk retry + authoritative post-call guard) pass. | Closes the other half of RC-2 (RTR-01 fixes extraction; RTR-02 ensures search actually runs). | +0-1 per chunk when retry fires (bounded) | +0-5% | Done |
| **VER-01** (Re-verify sources added post-stage-06) | fix | Stage 08 recovery (`engine/pipeline/stages/08-recover.js`) appends new sources to cells AFTER stage 06 source verification has already run. Those sources never pass through `verifySource` → they have no `verificationTier`. In the 2026-04-20T18 bundle, 91 of 176 relevant sources (52%) had empty `verificationTier` and fell through to the legacy `notFound` fallback in [engine/lib/guards/decision-gate.js:114-117](engine/lib/guards/decision-gate.js:114): `unresolvedRatio` inflates, SQ-13 fabrication gate fires, and otherwise-accurate recovered evidence looks indistinguishable from fabrication. | 1) Factor the per-source verification logic out of [engine/pipeline/stages/06-source-verify.js](engine/pipeline/stages/06-source-verify.js) into a new helper `engine/lib/sources/verify-source.js` exporting `verifySource({url, options})` returning `{verificationTier, citationStatus, httpStatus, lastChecked, rawError?}`. Stage 06 imports and uses it; behavior unchanged there. 2) In [engine/pipeline/stages/08-recover.js](engine/pipeline/stages/08-recover.js), after the recovery-source append step, iterate newly-added sources (those without `verificationTier`) and call `verifySource` with the same options stage 06 uses (concurrency cap, timeout from `STAGE_BUDGETS.stage_06_source_verify`). Persist tiers onto the source objects in place. 3) In `decision-gate.js` `citationCoverageMetrics` (lines 91-118), delete the legacy `normalizeCitationStatus` fallback at lines 114-117. An untiered source is now a data bug, not a normal state. 4) When any source still lacks `verificationTier` after stage 08, emit reason code `source_missing_verification_tier` with `{sourceCount, sampleCellIds: [...]}`. Treat untiered sources as `unverifiable` (neutral) in the gate, NOT `notFound` (unresolved). 5) Unit test: build a state with 5 recovery-added sources (3 valid URLs + 2 fabricated); run stage 08; assert all 5 have `verificationTier`, 3 = `verified`, 2 = `fabricated`. Grep-verify no remaining reference to the removed fallback branch. | Rerun: `source_missing_verification_tier` count is 0 OR a tiny residual (≤ 2% of sources, attributable to genuine verifier timeouts). `notFound` tier count drops from ~52% to 0%. Gate `unresolvedRatio` reflects real fabrication + stale only, not pipeline drift. Legacy fallback code removed. | Restores correct signal separation between "URL unreachable" and "URL never checked". | +1 verify pass per recovery source (bounded; `RECOVERY_MAX_CELLS` is already capped) | +2-5% | Done |
| **DEF-01** (Stage 13 missing-response handling) | fix | Stage 13 defend in the failing bundle: `outcomes: 5, missingResponses: 1, noteMissingCount: 0`. Model returned fewer outcome objects than input flags — one critic flag got no response. That flag silently counts toward `unresolvedCount` in `unresolvedCriticCounts` → gate's `criticResolution` check fails on `maxUnresolvedCriticFlags: 0`. Gate message says "critic unresolved" when the real cause is a pipeline bug (model output missing expected entries). Adjacent to but distinct from ENG-13 (ENG-13 is empty-note → silent disposition flip; DEF-01 is missing-outcome → silent unresolved count). | 1) In [engine/pipeline/stages/13-defend.js](engine/pipeline/stages/13-defend.js) `normalizeOutcome` (or wherever outcomes are reconciled against input flags), detect `returnedOutcomes.length < inputFlags.length` and retry the stage call once with an explicit prompt reminder: "you must return exactly one outcome object per input flag, including `flagId`; missing flagIds: [...]". 2) On second miss, for each still-missing flag set `resolved: false`, `analystNote: "retry exhausted — no response returned"`, `dispositionOverride: "unresolved_missing_response"`, and emit reason code `defend_missing_response` with `{missingFlagIds: [...], stageAttempts: 2}` detail. 3) Extend `unresolvedCriticCounts` in [engine/lib/guards/decision-gate.js:48](engine/lib/guards/decision-gate.js:48) to track `missingResponseCount` separately from `unresolvedHighCount`. 4) Add a new gate check `criticDefendCompleteness` that passes iff `missingResponseCount === 0`. Fails distinctly from `criticResolution`. 5) Unit test: inject a critic outcomes array omitting one flag; observe one retry + `defend_missing_response` reason code + `criticDefendCompleteness: false` in the gate output. Gate-failure message lists which flags were missing. | Rerun: zero `missingResponses` on stage 13. Gate output distinguishes "critic unresolved" (evidence gap) from "defend missing response" (pipeline bug). Unit test passes. | Removes a class of silent pipeline→evidence misattribution. Extends "no silent failure" to stage 13. | +0-1 per affected run (retry bounded) | +0-3% | Done |
| **SYN-01** (Stage 14 synthesize: remove silent compaction) | fix | Stage 14 synthesize in the failing bundle emitted `prompt_token_over_budget` + `prompt_compaction_applied` reason codes. The prompt exceeded its 6000-token input budget and was silently compacted (evidence dropped) before the Gemini call. Output was only 1300 of a 6000 cap. Matrix synthesis ran on truncated evidence for the final decision-grade step. This violates quality-bar.md "Preserve evidence content; do not drop valid model output due to schema mapping drift." 9×8 matrices with rich per-cell evidence routinely exceed 6000 tokens; the cap is structurally wrong for this stage. Additional root cause in code: [engine/pipeline/stages/14-synthesize.js](engine/pipeline/stages/14-synthesize.js) currently hard-truncates the prompt payload with `.slice(0, 32000)` and `.slice(0, 14000)` before token-preflight runs. | 1) In [engine/pipeline/stages/14-synthesize.js](engine/pipeline/stages/14-synthesize.js), remove hard prompt truncation (`.slice(0, 32000)` / `.slice(0, 14000)`) from synthesis prompt assembly. No silent byte drops are allowed. 2) In [engine/pipeline/stages/14-synthesize.js](engine/pipeline/stages/14-synthesize.js) and [engine/pipeline/orchestrator.js](engine/pipeline/orchestrator.js) `STAGE_BUDGETS`: raise stage 14 token budget to a matrix-safe value (120000 input target with 16000 output reserve); keep smaller scorecard defaults. 3) In [engine/lib/guards/token-preflight.js](engine/lib/guards/token-preflight.js), add `stage.allowCompaction` (default `true` elsewhere) and set `false` for stage 14. If stage 14 still overflows budget, emit `stage_14_oversize_abort` and hard-abort — do NOT compact. 4) Option B (fallback when monolithic prompt still too large): chunk matrix synthesis by subject-group (e.g., 3 subjects × 8 attrs) via CHUNK-01 manifest, then deterministically merge chunk summaries. No compaction path in either mode. 5) Unit test 1: state >6000 but <120k fits stage 14 with no compaction and no truncation reason codes. Unit test 2: >120k aborts with `stage_14_oversize_abort`. Unit test 3: chunked strategy covers all subjects in output. | Rerun of 9×8 matrix: zero `prompt_compaction_applied` reason codes on stage 14 and no `.slice(...)` truncation in synthesis prompt path. Synthesis output references every subject and evidence set. Unit tests pass. | Direct quality-bar compliance. Synthesis is the stage users read first — it MUST NOT operate on silently dropped data. | 0 runtime; larger prompts on stage 14 only | +5-15% on stage 14 tokens | Done |
| **GATE-01** (`fabricationSignal: unknown` when grounded set unavailable) | fix | `fabricationSignalFromSources` in [engine/pipeline/stages/common.js](engine/pipeline/stages/common.js) computes `high` whenever `groundedSourceCount === 0`. When RTR-01/RTR-02 preconditions fail (extractor broken OR search skipped), every source is marked `groundedByProvider: false` and the signal is structurally `high` — not because sources are fabricated but because instrumentation is absent. SQ-13 gate then fails on a 100% fabrication rate driven by instrumentation, not evidence. | 1) In `fabricationSignalFromSources`, detect the "grounded set unavailable" condition explicitly: `liveSearchUsed === false` OR (`groundedSourceCount === 0` AND `callFailedGrounding === true`, where `callFailedGrounding` is a new meta bit set by RTR-01 when `webSearchCalls > 0` AND extraction returned 0). Return `fabricationSignal: "unknown"` in that case, not `"high"`. 2) Persist `fabricationSignalReason: "grounded_set_unavailable"` alongside the signal so UI + debug bundle can distinguish. 3) In [engine/lib/guards/decision-gate.js](engine/lib/guards/decision-gate.js) `citationCoverageMetrics`, count `unknown` separately — exclude from `unresolvedNumerator` and from `fabricationRatio` denominator (neutral). Surface `unknownRatio` in gate summary. 4) When `unknownRatio > 0.5`, emit reason code `source_grounding_unavailable` (diagnostic; does NOT fail the gate on its own — the RTR-01/RTR-02 infrastructure failures do). 5) UI: source badge renders `unknown` as "grounding unavailable" not "likely fabricated". 6) Unit test: build state with `liveSearchUsed: false` across 5 sources → all 5 `fabricationSignal: "unknown"`, `fabricationSignalReason: "grounded_set_unavailable"`. Gate `fabricationRatio === 0` (not 1.0), `unknownRatio === 1.0`, `source_grounding_unavailable` emitted. | Synthetic test above passes. Rerun: `fabricationSignal: "unknown"` appears where appropriate; `fabricationRatio` reflects verified-vs-fabricated only, not presence/absence of instrumentation. UI shows the correct label. | Prevents decision gate from masking infra bugs as evidence bugs (the exact failure mode of the 2026-04-20T18 run). | 0 | 0% | Done |
| **PLN-01** (Planner unit-count assertion) | fix | Stage 02 in failing bundle: `plannerReturnedUnits: 8` on a 9×8 matrix request, `retries: 1`, `parseFailureTruncationSuspected: true`. Important clarification: stage 02 plans at ATTRIBUTE level for matrix mode ([engine/pipeline/stages/02-plan.js:7-14](engine/pipeline/stages/02-plan.js:7) — one unit per attribute, never per subject), so 8 is the correct expected count for an 8-attribute matrix. The real signal from the failing bundle is `parseFailureTruncationSuspected: true` from INFRA-02 — the plan response was truncated mid-parse; next time truncation hits, an attribute unit could be silently dropped (7 of 8 returned), leaving one attribute uninvestigated across every subject. No assertion today catches this. | 1) In [engine/pipeline/stages/02-plan.js](engine/pipeline/stages/02-plan.js), after `normalizePlan`, compute `expectedUnitCount`: for matrix mode = `request.matrix.attributes.length`; for scorecard = `request.scorecard.dimensions.length`. 2) Assert `plannerReturnedUnits === expectedUnitCount` (use the existing `plannerReturnedUnits` from `summarizePlanInputDiagnostics` at line 97; count pre-normalization so dropped units are detected, not masked by the fallback-query injection). 3) On mismatch, retry the planner call once with explicit reminder: "return exactly N units, one per input attribute; missing unitIds: [...]". 4) On second mismatch, abort stage 02 with reason code `plan_unit_count_mismatch` carrying `{expected, returned, missingUnitIds: [...], firstAttemptCount, secondAttemptCount}`. 5) Independently of count match: when INFRA-02's `parseFailureTruncationSuspected: true` fires on the planner call, ALWAYS retry the call once even if count matches — truncation after parse-repair is a silent-loss signal that may have happened to land on the right count by coincidence. Emit `plan_truncation_retried` reason code. 6) Decision gate (`decision-gate.js`): `plan_unit_count_mismatch` is a structural abort classifier, tagged as `pipeline_structural_failure` per quality-bar, not a gate check. 7) Unit test 1: planner returns 7 units on an 8-attribute matrix request → observe one retry, second call returns 8 → success, zero final reason codes. Test 2: second call also returns 7 → abort with `plan_unit_count_mismatch`, `missingUnitIds` populated. Test 3: `parseFailureTruncationSuspected: true` on a count-correct 8-unit response → retry forced, `plan_truncation_retried` emitted. | Rerun of 9×8 matrix: `plannerReturnedUnits === 8`; zero `plan_unit_count_mismatch`; `plan_truncation_retried` fires only if truncation genuinely suspected. Unit tests pass. | Closes truncation-driven unit drop. Companion to ENG-11 (empty-query fallback) but different surface: PLN-01 catches INFRA-02-detected truncation that would drop attribute units. | +0-1 per affected run (retry bounded) | +0-5% | Done |
| **PAR-01** (Parallelize 03a/03b chunk execution) | performance | Stages 03a and 03b execute chunks serially via `while (queue.length) { ... await callActorJson ... }` loops in [engine/pipeline/stages/03a-evidence-memory.js:180-296](engine/pipeline/stages/03a-evidence-memory.js:180) and [engine/pipeline/stages/03b-evidence-web.js:288-408](engine/pipeline/stages/03b-evidence-web.js:288). Failing bundle showed 14.9 min on 03a and 28 min on 03b for 6 chunks each — total ~43 min where 3-4 way concurrency would cut it roughly in half. CHUNK-01 made parallelization safe: chunks are stable cell-coordinate slices (`c01`..`c06`, canonically sorted); merge is deterministic by `subjectId::attributeId` key in `cellsByKey`; no inter-chunk dependencies exist (each call is independent per cell group). Serial execution was a pre-CHUNK-01 scheduling convenience, not a correctness requirement. Must not regress quality. | 1) Replace the `while(queue.length)` loop in both stages (and [engine/pipeline/stages/08-recover.js](engine/pipeline/stages/08-recover.js) if it uses the same pattern) with a bounded worker-pool pattern. Implement as a shared helper `engine/lib/runtime/chunk-pool.js` exporting `runChunkPool({initialChunks, processChunk, concurrency, onSplit, onAbort})`. Workers pull from a shared queue; on chunk failure the worker runs the CHUNK-01 split logic and enqueues children. Pool drains when queue empty and no workers active. 2) `STAGE_BUDGETS` new field `chunkConcurrency`: default 3 for `stage_03b_evidence_web`, 4 for `stage_03a_evidence_memory` (memory is faster/cheaper), 3 for `stage_08_recover`. Per-stage tunable; caps loaded from env (`RESEARCHIT_STAGE_03B_CHUNK_CONCURRENCY` etc.). 3) Provider rate-limit guard: new env `RESEARCHIT_GEMINI_RPM` (default 60); pool throttles when combined outstanding calls across stages approaches limit. Use a simple token-bucket in `engine/lib/runtime/rate-limiter.js`. Must coordinate across 03a+03b workers. 4) Deterministic output preservation: `cellsByKey` already keys by coordinate, not insertion order — reorder doesn't affect state. BUT `diagnostics[]`, `chunkTrace[]`, `chunkManifest` arrays are written by workers in completion order. Fix: each worker returns its own per-chunk objects; merge at pool drain and sort by `chunkId` lexicographically before emit. This makes debug bundles bit-stable across runs and diffable between serial-vs-parallel baselines. 5) Abort propagation: pool accepts a parent `AbortController`; workers each register their own call's `AbortController` with `source: "orchestrator_cancel"` via INFRA-01. On any chunk split-exhausted terminal error, pool cancels all in-flight siblings and bubbles the error. On user cancel, all workers terminate cleanly within `deadlineMs`. 6) RES-01a cache interaction: cache lookup per chunk still runs inside each worker pre-call (fast; no change). Cache write is per-chunk post-success; no global serialization needed. 7) Chunk split semantics unchanged: children still go to the pool's shared queue. Depth tracking unchanged. 8) Observability: `diagnostics.chunkConcurrency` (configured value) and `diagnostics.peakWorkerCount` (max simultaneously active workers observed) emitted per stage. 9) Unit test 1: pool with `concurrency: 3`, 10 synthetic chunks each `await 1s` mock → total wall-clock ~4s (10/3 rounded up), not 10s. Test 2: rate-limit test with `RESEARCHIT_GEMINI_RPM: 10` and 20 chunks → pool throttles; peak worker count ≤ 2-3 at any moment despite concurrency 5. Test 3: abort test — kill one worker mid-run → sibling workers terminate within deadlineMs, pool bubbles error. Test 4: quality regression fixture — same 9×8 healthcare input run serial (`concurrency: 1`) vs parallel (`concurrency: 3`) produces bit-identical `cellsByKey`, identical `chunkManifest`, and `diagnostics[]` sorted identically by `chunkId`. | Integration rerun: stage 03a duration drops ≥40% vs current baseline; stage 03b drops ≥35%. `cellsByKey` bit-identical to serial run on the regression fixture. Diagnostics + chunkTrace sorted by `chunkId` ascending. Abort + rate-limit tests pass. Zero increase in `fabricationSignal: high` rate vs serial baseline (confirms no quality regression from concurrency). | Depends on: INFRA-01 (done — abort wiring); CHUNK-01 (done — stable chunk IDs + deterministic merge). Blocks: none directly, but lowers cost of Tier 1.5 revalidation cycles. | 0 new provider calls | 0% (same total spend, faster wall-clock) | Done |
| **UI-01** (Progress tab blocked-state regression on terminal errors) | fix | On failures that set `phase: "error"` in [app/src/App.jsx:736](app/src/App.jsx:736) and [app/src/App.jsx:822](app/src/App.jsx:822), [app/src/components/ProgressTab.jsx:358](app/src/components/ProgressTab.jsx:358) marks every stage `failed`/`Blocked` because `currentIdx` resolves from a non-flow phase and the fallback condition `currentIdx <= idx` is true across the flow. This overwrites true stage outcomes (done/cached/failed) and broke forensic UX in the 2026-04-20T18 run. | 1) In `ProgressTab`, derive `currentIdx` with a sentinel (`-1`) when `resolvedPhase` is not in the flow map; do not default to `0`. 2) In `getStepState`, keep `stageRecord.status` authoritative (`done/cached/failed`) and only use the `uc.status === "error"` fallback when `currentIdx >= 0`. 3) Ensure completed/cached stages remain checked after terminal failure; only the failed stage and later not-started stages render blocked. 4) Add regression test/snapshot for a run where diagnostics show stages 01-14 complete and stage 15 failed while `uc.phase = "error"`; expected UI preserves 01-14 done. | Reproducing failure with `phase: "error"` no longer flips all stages to Blocked. Resume UX correctly indicates the real failed stage (e.g., stage 14/15), and previously completed stages stay checked/cached. | Restores trustworthy run forensics and resume targeting; prevents user confusion about where failure actually occurred. | 0 | 0% | Done |
| **GND-01** (Stage → cell grounded-metadata propagation) | fix | Debug bundle `analysis-debug-bundle-2026-04-21T12-46-27-314Z.json`: 277 grounded sources emitted at stage-level (`stage_03b_evidence_web`) but `groundedByProvider: false` appears 133 times on cell-level `sources[]`. RTR-01 correctly extracts from Gemini's `groundingMetadata`; SQ-10 correctly populates chunk-level `meta.groundedSources[]`; the gap is between chunk-meta and per-cell source construction in [engine/pipeline/stages/03b-evidence-web.js](engine/pipeline/stages/03b-evidence-web.js) `gatherMatrixWeb` cell-builder — each `cell.sources[i]` is built from the model's cited URL but `groundedByProvider` is not looked up against the chunk's `meta.groundedSources[]` set. Downstream: FAB-01's three-criteria check cannot distinguish "URL is in grounded set but plumbing dropped the flag" from "URL is truly ungrounded". | 1) In [engine/pipeline/stages/03b-evidence-web.js](engine/pipeline/stages/03b-evidence-web.js), after each per-chunk call returns, build `groundedUrlSet = new Set(meta.groundedSources.map(s => canonicalizeUrl(s.url)))`. 2) When constructing each `cell.sources[i]`, look up `canonicalizeUrl(source.url)` in that set: if present, set `source.groundedByProvider = true` and `source.groundingConfidence = "provider"`; if absent, set `source.groundedByProvider = false` and `source.groundingConfidence = "model-emitted"`. 3) Same pattern in [engine/pipeline/stages/03a-evidence-memory.js](engine/pipeline/stages/03a-evidence-memory.js): memory stage has no grounded set, so all sources get `groundedByProvider: false, groundingConfidence: "memory"`. Memory-cited sources must NOT be treated as fabricated by FAB-01 — three-criteria still applies. 4) Same pattern in [engine/pipeline/stages/08-recover.js](engine/pipeline/stages/08-recover.js) recovery path. 5) Diagnostics: emit `diagnostics.groundingPropagation: {stage, totalSources, groundedByProviderTrue, groundedByProviderFalse, groundingConfidenceDistribution}` per stage. 6) Unit test 1: synthetic 03b chunk with 10 grounded URLs in stage-meta; model cites 8 of those + 2 outside set → 8 `groundedByProvider: true`, 2 `false`. Test 2: 03a memory stage → every source `groundedByProvider: false, groundingConfidence: "memory"`. Test 3: after RETR-01 lands, corpus-id sources all propagate as `true` via the corpus path. | Rerun: `groundedByProvider: true` count at cell-level matches stage-meta grounded count within ±5%. Zero "stage-meta says grounded but cell says not grounded" discrepancies (grep-verified on bundle). `groundingPropagation` diagnostic present on stages 03a/03b/08. Unit tests pass. | Prerequisite for FAB-01 definition to work correctly. Closes the "277 grounded but cell-level says 133 ungrounded" gap from T12-46 bundle. | 0 | 0% | P0 |
| **FAB-01** (Fabricated-tier three-criteria definition) | fix | Current fabricated tier fires whenever `groundedByProvider === false` (via `fabricationSignalFromSources` in [engine/pipeline/stages/common.js](engine/pipeline/stages/common.js) + classifier in [engine/pipeline/stages/06-source-verify.js](engine/pipeline/stages/06-source-verify.js)). quality-bar.md explicitly states fabrication requires THREE criteria: (1) URL absent from provider's grounding set, (2) 404 on the subject's own domain, AND (3) citation contradicted by retrieved content. Current definition = criterion 1 alone. Result in T12-46 bundle: stage 06 tier counts `verified: 60, fabricated: 235, unreachable_infrastructure: 11, unverifiable: 15` — `fabricated` wildly over-counted; many are "not-in-grounded-set but HEAD-reachable and on vendor's own domain" which by the quality-bar should be `unverifiable` at worst. Direct violation of "Maximum Extraction, Not Minimum Filtering". | 1) New helper `engine/lib/sources/fabrication-tier.js` exporting `classifyFabricationTier({url, groundedByProvider, httpStatus, domain, subjectDomain, contentContradicts?})`. Decision tree per quality-bar.md: (a) `httpStatus < 400` AND `groundedByProvider === true` → `verified`; (b) `httpStatus < 400` AND `groundedByProvider === false` AND `domain === subjectDomain` → `unverifiable` (NOT fabricated; model may have retrieved before search-set was recorded); (c) `httpStatus === 404` AND `domain === subjectDomain` → `fabricated` (criteria 1+2 met); (d) `httpStatus === 404` AND `domain !== subjectDomain` AND `groundedByProvider === false` → `fabricated` (wholly invented); (e) transport noise (`403/429/paywall/timeout`) → `unreachable_infrastructure`; (f) otherwise → `unverifiable`. 2) [engine/pipeline/stages/06-source-verify.js](engine/pipeline/stages/06-source-verify.js) imports helper; replace inline classification with call. `fabricationSignalFromSources` in `common.js` uses same helper for consistency. 3) Criterion 3 (content-contradiction) is BEST-EFFORT for v1: when `retrievedCorpus` exists post-RETR-01, diff cited claim text against corpus entry; emit `contentContradicts: true|false|"unchecked"`. Pre-RETR-01, criterion 3 is always `"unchecked"` and classifier prefers `unverifiable` over `fabricated` (Maximum Extraction). 4) [engine/lib/guards/decision-gate.js](engine/lib/guards/decision-gate.js) `citationCoverageMetrics`: `fabricationRatio` denominator counts only tier `fabricated`; add `unverifiableRatio` alongside as diagnostic-only (NOT gate-failing). 5) Unit tests: (a) `innovaccer.com/old-page`, HEAD 404, domain matches → `fabricated`. (b) `innovaccer.com/new-product`, HEAD 200, not in grounded set → `unverifiable`. (c) `randomhallucinated.example.com`, HEAD 404, domain mismatch, not grounded → `fabricated`. (d) URL with 403 → `unreachable_infrastructure`. (e) T12-46 re-classification fixture — fabricated count drops from 235 to expected 20-50 range. | Rerun: stage 06 `fabricated` tier count ≤ 10% of source population (was 75% in T12-46). `unverifiableRatio` surfaced separately. Gate threshold fires on real fabrication only. Unit tests pass; T12-46 re-classification matches expected drop. | Direct compliance with quality-bar.md three-criteria definition and Maximum Extraction principle. Depends on GND-01 (grounded flag must be trustworthy to use as criterion 1). | 0 | 0% | P0 |
| **GND-02** (Stage 08 recovery fabrication audit) | fix | T12-46 bundle stage 08 recovery: 237 of 244 recovered sources tagged `fabricated`. Extreme skew suggests (a) recovery Gemini call with `liveSearch: true` isn't reading recovery-call grounded set into the cell-level propagation path (same class as GND-01 but recovery-specific); OR (b) recovery prompt induces URL invention at higher rate than 03a/03b. Not separable from FAB-01 + GND-01 until they land; GND-02 is the stage-08-specific confirmation + fix. | 1) Audit [engine/pipeline/stages/08-recover.js](engine/pipeline/stages/08-recover.js) recovery-search call: confirm `meta.groundedSources[]` is captured per recovery chunk and propagated into each newly-added source (per GND-01 contract). 2) VER-01 re-runs `verifySource` for HTTP status but not for `groundedByProvider`; fix: after recovery-append + `verifySource`, also set `groundedByProvider` from the recovery-call's grounded set. 3) Add diagnostic `diagnostics.stage08Recovery: {sourcesAdded, groundedByProviderTrue, groundedByProviderFalse, verificationTierCounts}` — makes over-flagging visible without diffing the full bundle. 4) If audit (after GND-01+FAB-01 land) still shows recovery prompt inducing URL invention, tighten recovery prompt: "you MUST call google_search for each missing-evidence claim; do not propose URLs without calling search first". 5) Unit test: synthetic recovery chunk with 5 grounded + 2 model-emitted URLs → 5 `groundedByProvider: true`, 2 `false`; post-FAB-01 tier classification yields 5 `verified` + 2 `unverifiable`. | Rerun: stage 08 recovery fabricated-tier ratio ≤ 15% (was 97% in T12-46). `stage08Recovery` diagnostic present. No legitimate recovery URL mis-tiered as fabricated. | Depends on GND-01 + FAB-01. RETR-01 extended to stage 08 makes this obsolete by construction — at which point GND-02 becomes a verification task rather than a separate fix. | 0 | 0% | P0 |
| **GATE-02** (Decision-gate emission visibility audit) | fix | T12-46 bundle grep for `fabricationSignal` token: **zero hits** across 26211 lines. Either (a) GATE-01 (Tier 1.5) didn't actually emit the field as intended, or (b) it emits under a different key and the bundle serializer drops it. Without visibility the gate's authoritative fabrication classification is untestable — a gate that runs but whose output isn't diffable is effectively silent failure. Must be fixed before any FAB-01/GND-01 rerun validation. | 1) Debug-bundle contract test at `engine/__tests__/debug-bundle-contract.test.js`: after a synthetic run, every decision-gate field (`fabricationSignal`, `fabricationSignalReason`, `fabricationRatio`, `unknownRatio`, `unverifiableRatio`, `verifiedRatio`) MUST appear in `debugBundle.stages.stage_15_finalize.decisionGate` at the documented key. Fail fast on missing keys. 2) Audit [engine/lib/diagnostics/debug-bundle.js](engine/lib/diagnostics/debug-bundle.js) serializer for decision-gate output: ensure all gate-computed fields are serialized, not just the gate boolean + reason codes. 3) Audit [engine/lib/guards/decision-gate.js](engine/lib/guards/decision-gate.js) `citationCoverageMetrics`: confirm emitted object shape matches serializer expectations; explicit structure `decisionGate.citationCoverage.{fabricationSignal, fabricationSignalReason, fabricationRatio, unknownRatio, unverifiableRatio, verifiedRatio}`. 4) Emit `diagnostics.decisionGateEmissionCheck: true` on stage 15 when all expected keys present (self-verification). 5) Reason code `debug_bundle_contract_violation` if serializer drops any expected field (fires in test, non-blocking in prod). 6) Unit test: synthetic run with known gate state (verified: 60, fabricated: 20, unverifiable: 30) → bundle contains exact numeric fields; grep for `fabricationSignal` finds ≥1 match. | Rerun: grep `fabricationSignal` in bundle finds ≥1 match. All expected decision-gate fields present at documented keys. Contract test passes. | Forensics prerequisite for all FAB-01/GND-01 validation. Without this, reruns may appear to pass gates whose diagnostics are still silently dropped. | 0 | 0% | P0 |
| **RES-02** (Raw provider-response cache for 03a/03b/08) | feature | Post-processing bugs on retrieval stages (e.g. RTR-01 broken `extractGeminiGroundingSources`) require re-calling the expensive provider to reprocess. Stage 03b cost ~$1-3 per run and took 28 min in the failing case. Today `callActorJson` returns only `{text, parsed, meta: {groundedSources: extracted_here}, tokenDiagnostics}` — the raw provider payload (Gemini `groundingMetadata.groundingChunks[]`, full `usage`, `finishReason`, tool-call records; OpenAI `choices[]`, `finish_reason`; Anthropic `content[]`, `stop_reason`) is discarded after extraction. RES-01a caches normalized stage OUTPUTS keyed by `promptVersion`; a normalizer bugfix implies a prompt-version bump that invalidates that cache → forced re-call. Raw-response cache is orthogonal: it lets the extractor/normalizer be re-run over cached raw payloads without paying for the LLM again. Also makes forensics self-sufficient (the 2026-04-20T18 investigation would have gone from inference to direct inspection). Ship FIRST in Tier 1.6 — RETR-01 build iterates on retrieve sub-stage extractor; without RES-02 each iteration pays full Gemini cost. | 1) In [app/api/providerCalls.js](app/api/providerCalls.js), after each provider call on stages `stage_03a_evidence_memory`, `stage_03b_evidence_web`, `stage_08_recover` (stage id passed through from `callActorJson` context), persist the raw provider JSON payload to Upstash via [app/api/_lib/store.js](app/api/_lib/store.js). New namespace `ri:run:{runId}:rawcall:{stageId}:{chunkId}:{callIndex}`. Value shape: `{ provider, model, stageId, chunkId, callIndex, requestHash, rawResponse, storedAt, promptVersion }`. `rawResponse` = full provider payload (Gemini: `{candidates, promptFeedback, usageMetadata, modelVersion}`; OpenAI: `{id, object, model, usage, choices, finish_reason, ...}`; Anthropic: `{id, type, role, content, model, stop_reason, usage}`). 2) Redaction before store: strip `authorization`, `api-key`, session cookies, `x-goog-api-key` from anything header-shaped; preserve all content/metadata. Use an allowlist-based passthrough, not a regex blacklist. 3) Scope v1: stages 03a, 03b, 08 only. Critic + scoring stages excluded (cheaper + less post-processing complexity). Config flag `RESEARCHIT_RAW_CALL_CACHE_STAGES="stage_03a_evidence_memory,stage_03b_evidence_web,stage_08_recover"` gates scope. `RESEARCHIT_RAW_CALL_CACHE_DISABLED=1` disables entirely. 4) TTL: 7 days (matches RES-01a stage cache). Share `_lib/store.js` TTL helper. 5) New API route `app/api/account/raw-calls/[runId]/[stageId].js` returning `{calls: [{chunkId, callIndex, rawResponse, storedAt}, ...]}` — require caller auth (runId-owned). 6) New orchestrator entry `engine/pipeline/orchestrator.js` export `reprocessStage({runId, stageId, runtime})` that: loads raw responses via API; runs ONLY the stage's extract/normalize/merge steps against the cached raw payload (no provider call); updates stage output in `state.resolved`; re-runs downstream stages from `stageId + 1` through 15. Useful for: fixing RTR-01 after the patch lands on stored runs, retro-fitting VER-01 over previously-run stage 08 outputs, validating SYN-01 without paying for retrieval. 7) Debug bundle emits per-call `rawResponseKey` (the Upstash key string, not the payload — too large). CLI `npm run fetch-raw <runId> <stageId>` pulls raw responses into `tmp/raw-calls/{runId}/{stageId}/{chunkId}-{callIndex}.json` for local inspection. 8) Storage sizing: Gemini raw response with 20 grounding chunks ≈ 30-80 KB; 9×8 matrix × 6 chunks × 3 stages × 7 days ≈ 1-2 MB per run. Upstash free tier (256 MB) accommodates >100 in-window runs. 9) Unit tests: (a) post-call store fires on stage 03b, payload redacted of auth headers, readable via API; (b) `reprocessStage("stage_03b_evidence_web", runId)` runs normalizer over cached raw, produces updated output with zero new provider calls; (c) synthetic broken extractor → bug run stored → fix extractor → reprocess → correct output. | Stage 03b call stores raw payload under `ri:run:{runId}:rawcall:stage_03b_evidence_web:{chunkId}:0`. `reprocessStage` runs end-to-end with zero new provider calls for the 03a/03b/08 stages. Debug bundle includes `rawResponseKey` per call. CLI `fetch-raw` tool works. Redaction unit test: stored payload contains no `authorization`/`api-key` fields. Storage per-run ≤ 2 MB verified on a 9×8 matrix integration run. | Dependency: none (reuses RES-01a's Upstash infrastructure). Complements (not duplicates) RES-01a — RES-01a: stage-OUTPUT cache, invalidates on prompt change. RES-02: stage-INPUT cache (raw provider response), invalidates only on model/provider change. | 0 runtime | +0-2% (tiny write amplification) | P0 |
| QG-04 (Reconcile acceptance gate) | fix | Stage 04 reconcile can be accepted without measurable lift; `contradictionReduced = true` is a placeholder that weakens rejection. | Replace placeholder with real reconcile lift metrics: minimum low-confidence reduction, source-coverage lift, contradiction delta. If unmet, reject reconcile, retain best prior draft, regenerate targeted plan, flag reconcile-health failure. | On synthetic input where reconcile adds no lift, reconcile is rejected; prior draft retained; `reconcile_rejected_no_lift` reason code emitted; diagnostic shows specific unmet metric. On a real-lift input, reconcile accepted. | Prevents reconcile from locking in low-information outputs. | 0 | 0% | Done |
| DA-05 (Async orchestration + run persistence) | feature | Closing/reloading browser tabs can drop in-flight progress visibility; long multi-provider runs feel lost. Complementary to RES-01 (which is about post-failure cache, not in-flight state). | Durable async run orchestration with persisted run state, resumable steps, cancellation, idempotent retries for Native and Deep Research ×3. Progress events survive reconnects; recover by `runId`. | Close browser mid-run, reconnect by `runId` → progress UI reconstructs current stage + chunk state. Cancellation request on disconnected run cancels cleanly. Idempotent step retry on reconnect doesn't duplicate provider calls. | Foundation for account-backed run continuity; prerequisite for UA-03 library. | 0 quality infra | 0% | P0 |
| **RETR-01** (Retrieve-then-Reason: split stage 03b and stage 08) | architecture | Stages 03b and 08 today conflate retrieval and reasoning in one Gemini call — model receives prompt, internally calls `google_search`, emits cells with cited URLs. Model is free to invent URLs, misattribute claims, or drop grounded pages in favour of "remembered" ones. Tier 1 + Tier 1.5 fully instrument this path; T12-46 integration run confirmed the wire-level fixes landed but exposed two definitional bugs (GND-01, FAB-01) rooted in the same architectural conflation — "did this URL come from grounded retrieval?" has to be inferred via metadata plumbing that keeps breaking in new places. Promoted P1 → P0 because the architectural split makes fabrication-by-retrieval impossible by construction, collapsing several whack-a-mole tasks into one structural fix. | Restructure stage 03b and stage 08 into two deterministic sub-stages each. **03b.1 Retrieve:** model proposes queries only (no cells). Queries execute through Gemini `google_search` tool-use. Results captured into `retrievedCorpus: Array<{corpusId, url, canonicalUrl, title, snippet, query, rank, retrievedAt}>`. Any model output beyond queries rejected at parse time. **03b.2 Read:** model receives corpus as structured JSON context, produces cells. Constraint: `sources[].corpusId` must reference an entry in corpus; cells emitting URLs without `corpusId` dropped with reason code `source_absent_from_corpus`. Stage 08 follows same split (retrieve sub-chunk-scoped; read re-assesses the cell). Stage 14 synthesis reads only from aggregated corpus; synthesis cannot introduce new URLs. Retrieve sub-stage: (a) captures full `groundingMetadata` via existing RTR-01 extractor; (b) canonicalizes URLs once (SQ-09) and stores canonical form as corpus key; (c) stores raw call via RES-02. Read sub-stage: (a) receives corpus as explicit JSON input; (b) produces `sources[{corpusId, citedClaim, confidence}]`; (c) refuses to emit sources with `corpusId` not in input corpus. Keep Gemini for retrieve (per 2026-04-20 user decision); read sub-stage configurable per role (default gpt-5.4 for reasoning quality). Stage budgets: retrieve gets smaller token budget (queries only); read gets existing stage budget minus retrieve overhead. Ship in this order: RES-02 first (enables replay-debug), then GATE-02 visibility, then RETR-01 build; GND-01 + FAB-01 run in parallel (different files). | Zero cells emit URLs absent from stage's `retrievedCorpus` (grep-verified on rerun). `fabricationSignal: high` on retrieval-driven cells drops to ≤2% (irreducible floor: model emits corpus-id that maps to HEAD-404 URL). Stage 08 same. Retrieval call count + reasoning call count independently tunable. Token budget for read sub-stage drops (context corpus-scoped). 9×8 integration: fabricated-tier count ≤5% of sources (was 75% in T12-46). RES-02 raw-call store captures retrieve calls per cell. Unit tests: (a) retrieve sub-stage rejects model output containing cells; (b) read sub-stage rejects sources with unknown `corpusId`; (c) end-to-end produces matrix with 100% corpus-grounded citations on adversarial prompt. | Depends on: SQ-10 (Tier 1, done), RTR-01 (Tier 1.5, done), GND-01 (Tier 1.6 — corpus propagation uses same plumbing), RES-02 (Tier 1.5, open — raw-call store makes retrieve sub-stage cheap to iterate on). Blocks: CONF-01 (deterministic confidence uses corpus signals). Companion: FAB-01 (still needed for 03a memory path; RETR-01 only structurally fixes retrieval-driven cells, not memory-cited ones). | +1 per 03b cell (retrieve + read); stage 08 similar. Offset: read sub-stage prompt smaller (corpus-scoped) so per-call tokens drop. | +15-30% on stages 03b + 08; roughly neutral on total cost if RES-02 enables recovery replay | P0 |
| SQ-06 (Targeted claim verification via web search) | improvement | URL verification confirms a page is accessible but not that it contains the cited claim. A cell can fail URL verification while its claim is accurate; or pass verification while the cited page does not support the stated fact. | For cells still low-confidence after stage 08 recovery, extract 2-3 key specific claims and run targeted web searches to corroborate or contradict the claim itself (e.g. "Innovaccer 600 hospital customers"), not the URL. Store `claimVerificationStatus` per cell. Strictly bounded — only post-recovery low-confidence stragglers. | ≥30% of targeted cells see confidence upgraded after claim-corroboration search. `claimVerificationStatus` distribution tracked in diagnostics: `corroborated` / `contradicted` / `inconclusive`. Cell count targeted per run bounded to avoid unbounded search spend (e.g. ≤10 cells). | Orthogonal to Tier 1; closes gap between URL-verify and fact-verify. | +1-10 (bounded) | +5-30% (bounded) | P1 |
| ENG-09 (Stage 09 Rescore as LLM call) | fix | `docs/pipeline-architecture.md` declares Stage 09 as Analyst/gpt-5.4 rescore; actual `engine/pipeline/stages/09-rescore.js` is deterministic ±1 adjustment based on confidence counts — no LLM. Recovery evidence from stage 08 cannot re-interpret added sources/claims, only nudge numbers. Doc-code drift + real quality gap. | Implement rescore as an Analyst call. Inputs: (a) prior score + confidence + full, (b) recovery-added sources and claims, (c) critic flags pending. Model rewrites score + confidence with justification; deterministic guardrail bounds movement per iteration to prevent swings. Keep deterministic path as fallback if LLM call fails. | Stage 09 calls Analyst when recovery evidence exists; outputs rewritten score+confidence with justification. On LLM failure, deterministic fallback runs + emits `stage_09_fallback_deterministic` reason code. Movement bound (e.g. ±2 per iteration) enforced. | Closes documented-but-not-implemented stage. Gives recovery evidence real decision influence. | +1-2 | +5-10% | P1 |
| ENG-10 (Stage 15 Finalize run-summary LLM call) | fix | `docs/pipeline-architecture.md` says Stage 15 is "engine + Analyst (openai:gpt-5.4-mini) generate run summary"; actual `engine/pipeline/stages/15-finalize.js` has no `callActorJson` — it only assembles state and runs decision gate. UI/export has no pre-computed exec summary. | Add Analyst gpt-5.4-mini call to Stage 15 producing a short run-summary object: `{ decision, topEvidenceHighlights: [top 3], topRisks: [top 3], openQuestions: [top 2] }`. Persist on state as `runSummary`. Non-blocking: on failure or timeout, fall back to engine-computed summary so gate still runs. | `runSummary` present on every completed run. Schema validated. Fallback exercised in test by simulating LLM failure; gate still runs. | Matches doc; gives UI/export a pre-computed exec summary instead of ad hoc derivation. | +1 | +2-5% | P1 |
| ENG-11 (Stage 02 dead check arming) | fix | Stage 02 raises `CRITICAL_UNITS_UNRESOLVED` if `normalizePlan` returns units without queries, but `normalizePlan` inserts fallback queries for every unit → check unreachable. Planner failures go silent. | Option 1 (preferred): remove fallback query injection from `normalizePlan`; let the check actually fire when model returns no queries. Option 2: keep fallbacks but emit `plan_used_fallback_queries` reason code so real vs fabricated plans are distinguishable. | Option 1: planner failure reliably surfaces as `CRITICAL_UNITS_UNRESOLVED` in a test where model returns empty queries. Option 2: fallback usage emits reason code counted in diagnostics. | Removes false success signal from planner. | 0 | 0% | P1 |
| ENG-12 (Stage 04 preserve-evidence reconcile) | fix | `engine/pipeline/stages/04-merge.js` `shouldRejectReconcile` discards ALL web evidence when no numeric lift detected, emitting `RECONCILE_REJECTED_NO_LIFT`. Web evidence can add valuable sources/arguments even when scores don't move. | Change rejection: keep prior assessment scores+confidence, but merge new sources and supporting/limiting arguments from web. Only reject if new sources themselves low-quality (use SQ-12 fabrication signal). Emit `reconcile_scored_retained_evidence_merged` reason code. | On a test where web adds sources but no score lift, merged output has prior scores + web sources + web arguments. When web sources are fabrication-flagged, reject path fires with existing reason code. | Prevents silent evidence loss. Matches Maximum Extraction principle. | 0 | 0% | P1 |
| QG-05 (Gate-failure cause classification) | feature | When a run aborts on quality gates, debug bundle doesn't distinguish *why* gates failed. Forensic work on 2026-04-19 failing bundle required deep trace analysis to figure out that confidence was coerced + URLs hallucinated. quality-bar.md "Fatal" requires failures be actionable. | At stage 15 / decision-gate evaluation, classify each failed check into: `infrastructure_noise` (verifier 403/429 dominant), `pipeline_coercion` (e.g. `confidence_scale_coerced` count > N), `data_gap` (model expressed honest low-confidence; no grounded sources found), `fabrication` (fabricationSignal high). Emit `failureCauses: []` on run artefact. UI quality card on aborted runs. | Failing run shows `failureCauses` with specific cause(s); click-through forensics no longer needed for common cases. Each cause covered by at least one reason-code-producing signal. UI card renders the cause prominently on aborted runs. | Turns forensic investigations into click-through for gate-level failures. | 0 | 0% | P1 |
| RU-01 (Client crash-safety draft restore) | feature | Before full server-backed persistence, users can lose visible in-progress context after accidental tab close/refresh. | Persist lightweight in-progress snapshots locally; restore with resume/clear prompt on reload; unload guard when a run is active. | Accidental tab close during run: on reload, resume/clear prompt shows with correct run title and elapsed time. Unload guard confirmation on beforeunload while run active. | Immediate UX safety net while durable orchestration rolls out. | 0 | 0% | P1 |
| RU-03 (Run storage + retention + hard delete) | feature | Account features require explicit lifecycle rules for run artifacts and user-controlled deletion. | Durable run artifact storage model with retention metadata, hard-delete path, compatibility with existing export/import artifacts. | User-initiated hard delete removes all `ri:user:*:researches` entries + associated stage cache keys; verified via store inspection. Retention metadata present on every persisted run. Export/import round-trip preserved. | Enables trustworthy persistence + privacy controls before broad auth rollout. | 0 | 0% | P1 |
| UA-01 (Magic-link auth baseline) | feature | Returning users rely on local state; cannot reliably recover work across devices/sessions. | Email magic-link sign-in/up, session handling, account shell integrated with persisted run retrieval. | Email magic-link sign-in works end-to-end (request → email → verify → session). Session persists across devices. Existing persisted runs retrievable after sign-in. | Baseline identity layer for returning users + durable history. | 0 | 0% | P1 |
| UA-02 (Anonymous-to-account claim) | feature | Hard auth gate before users see value increases drop-off and can orphan anonymous runs. | Allow anonymous first run; claim recent runs after sign-up via secure ownership handoff. | Anonymous run completes; user signs up; claim flow transfers run ownership; anonymous session keys invalidated. Edge cases covered: claim after session expiry, claim conflict if user already has runs. | Keeps low-friction onboarding while preserving continuity after signup. | 0 | 0% | P1 |
| UA-03 (User research library + resume) | feature | Returning users need a reliable home for owned runs with status, continuation actions. | "My Researches" library with status filters (`running/completed/degraded/failed`), resume/open/delete/export actions, reconnect by `runId`. Resume here means "open and continue in-flight run" (DA-05-backed); for post-failure restart see RES-01b. | Library lists user's runs with correct status; filters work; actions exercise underlying APIs. Reconnect by `runId` restores in-flight state. | Makes Researchit usable as ongoing workspace, not single-session tool. Dependency: DA-05. | 0 | 0% | P1 |
| PAY-01 (Payments provider integration) | feature | Billing gates cannot work without reliable top-up checkout tied to account identity. | Integrate payment provider checkout/session return flow + account crediting entry point. Support test/sandbox and production modes. | Sandbox checkout → successful return → balance credited. Production mode validated against provider test suite. Failed payment handled gracefully. | Enables monetization and funding of paid runs. | 0 | 0% | P1 |
| PAY-02 (Balance ledger + usage charging + webhook reconciliation) | feature | Balance changes drift without canonical ledger and async payment reconciliation. | Append-only balance ledger; per-run debit records; idempotent webhook reconciliation; audit trail for credits/debits/refunds/adjustments. | Ledger entries immutable; balance == sum(ledger). Idempotent webhook replay doesn't double-credit. Audit trail queryable per user. | Accurate balances and trustworthy billing. | 0 | 0% | P1 |
| BIL-01 (Free run + balance gate policy) | feature | Onboarding/billing flow requires explicit gate rules tied to identity, payments, persisted runs. | Free-run entitlement policy; pre-run estimate checks; low-balance handling; export/top-up gates; UX messaging wired to account + ledger. | First-run free entitlement consumed correctly. Pre-run estimate blocks runs that exceed balance. Low-balance toast + top-up CTA. Export blocked when gate fails. | Aligns onboarding with operable monetization and cost controls. Depends on PAY-01, PAY-02. | 0 | 0% | P1 |
| FR-01 (In-app cost transparency) | feature | No-cap Deep Research ×3 policy needs clear visibility into expected/actual cost. | Show estimated range before launch; live spend/progress during run; actual by phase/provider in exports/debug logs. | Pre-launch estimate shown on setup modal. Live spend updates during run (per phase, per provider). Export + debug logs include per-phase actuals. | Preserves quality-first policy while avoiding surprise spend. | 0 | 0% | P1 |
| FR-04 (Per-run USD cost accounting by provider/model) | feature | Cannot yet compute trustworthy per-research + per-model spending; token/tool usage not normalized across providers nor persisted at step level. | Canonical usage ledger per run step: capture prompt/output/cached/tool tokens + search/tool calls from OpenAI/Anthropic/Gemini; normalize to common schema; versioned pricing table by provider/model/effective-date (env-overridable); compute USD per call, per phase, per provider/model, total per research; persist in run artifact + debug/export; show estimation confidence + mismatch note vs provider invoice. | Every step has normalized usage + USD attribution. Pricing table versioned, effective-dated. Estimation confidence surfaced. Total per-run cost matches provider invoice within documented tolerance. | Enables reliable spending analytics, pre-run estimate accuracy, and auditable billing. | 0 | 0% | P1 |
| UX-05 (Continue recovery action) | feature | Non-decision-grade runs show failure reasons but no direct one-click continuation. | "Continue Recovery" action that re-enters targeted recovery for failed checks only (bounded by configurable extra budget); re-evaluates decision-grade gate. | Button visible on non-decision-grade runs. Click re-enters targeted recovery only for failed checks. Budget cap enforced and visible. Gate re-evaluated after recovery. | Improves remediation loop; reduces full rerun cost. | +0-20 | +0-100% | P1 |
| UX-06 (Deep Research ×3 provider progress telemetry) | feature | Progress UI shows phase-level status but long deep-research-x3 runs don't expose live per-provider state during collection. | Emit provider-level progress snapshots (`queued/running/completed/failed`, duration, retries) during x3 collection; render in Progress tab for scorecard + matrix; persist in run metadata for reconnect visibility. | Progress tab renders per-provider state live during x3 runs. Snapshots persist and replay on reconnect. Retries/duration visible. | Better observability and lower user uncertainty during multi-minute runs. | 0 | 0% | P1 |
| FR-03 (Decision-grade acceptance suite) | feature | Decision-grade thresholds can regress without automated enforcement. | Fixture/live acceptance suite validating gate metrics + export/UI consistency for matrix and scorecard; fail CI on threshold regressions. | Suite runs on PR; gate metric regressions block merge; export/UI consistency checks included. Thresholds documented in repo. | Makes decision-grade guarantees testable/auditable. | benchmark-only | benchmark-only | P1 |
| CR-01 (Critic with web-search retrieval) | architecture | Critic stages 10-12 run Claude with no retrieval tools. SQ-05 expanded critic prompt to challenge factual accuracy, but without live evidence critic can only pattern-match against training (shares hallucination modes with Analyst). Cross-model fact-check only works if one model looks things up. | Give stages 10-12 bounded `web_search` budget via Anthropic Messages API: `max_uses: 3` coherence, `5` overclaims, `3` counter-case (tuned to p95 <30s/stage). Extend critic JSON schema: each flag gains `evidence: { citedClaim, correctingSource?, searchQueriesUsed[] }`. Plumb critic retrieved sources into `meta.groundedSources` (SQ-10 contract). Scope: ONLY factual-challenge flags — coherence/structural don't spend search budget. Feed critic's grounded sources into stage 13 defend. | Critic `factual` flags include `correctingSource` URL from Claude's own `web_search` in ≥60% of cases where Analyst cited factually outdated/wrong info. Manual review 3-5 runs: critic catches specific vendor-fact errors Analyst missed. Integration: Analyst hallucination → stage 11 `factual` flag with grounding URL → stage 13 defend takes it into account. | Depends on SQ-10. Extends SQ-05 from "prompt says find issues" to "architecturally capable of finding issues". | +1 search-enabled Claude call per cell with factual flags (budget-capped) | +15-25% per affected stage | P2 |
| DA-07 (Provider data governance + privacy) | feature | Three-provider Deep Research ×3 increases data handling risk (PII leakage, retention ambiguity). | Provider-level governance: redaction policy before provider calls; provider allow/deny list per run; retention policy metadata; explicit diagnostics about where data was sent. | Redaction runs before provider calls; test covers PII patterns. Allow/deny enforced. Retention + data-flow diagnostics present in every run artifact. | Trust/compliance foundation for enterprise usage. | 0 | 0% | P2 |
| DA-08 (Deep Research ×3 source provenance + ranked merge) | feature | x3 merge deduplicates sources but doesn't preserve provider-level provenance or rank by quality tier. | Extend merge: keep `citedByProviders[]` per source; deterministic ranking `independent > press > vendor` before confidence calibration + export rendering. Merge diagnostics in `analysisMeta` + debug artifacts. | Every merged source carries provenance. Ranking deterministic and verified on fixture. Merge diagnostics in debug bundle. | Stronger auditability and clearer trust weighting per claim. | 0 | 0% | P2 |
| ENG-13 (Stage 13 silent disposition flip) | fix | `normalizeOutcome` in `13-defend.js` requires non-empty `analystNote` to treat flag as `resolved: true`. If model returns `resolved: true` with empty note, silently flips to `rejected_with_evidence` — no reason code, no diagnostic. | Either (a) retry with explicit "include resolution note" instruction, or (b) keep as resolved with `analystNote: "model returned resolution without note"` + emit `defend_note_missing`. Never silently invert disposition. | Silent flip eliminated. Either resolved-with-note or diagnosable fallback. Reason code visible in diagnostics. | Removes silent data inversion. | +0-1 (retry) | +0-5% | P2 |
| ENG-14 (Unify assessment state path) | fix | `coverage-gate.js` reads `state.assessment`; `decision-gate.js` prefers `state.resolved.assessment` falling back to `state.assessment`. Post-stage-09 these can diverge → gates evaluate different data. | Canonical location (recommend `state.resolved.assessment` post-09, `state.assessment` pre). Both gates use shared accessor. Dev assertion flags drift if both populated and different. | Both gates read identical data. Drift assertion fires in test when paths disagree. | Removes "gates disagree silently" bug class. | 0 | 0% | P2 |
| RQ-25 (Model portfolio by function) | feature | One-model-heavy pipelines can share blind spots across phases. Multi-provider adapters missing despite routing infra (`withRoleModelOptions`) existing. | Route phases to role-specialized models with config overrides and fail-fast on pinned-route failure. Add multi-provider adapters for configured lanes only (no silent rerouting). | Role-specialized routes configurable; pinned-route failure fails fast with `route_mismatch_preflight`. Multi-provider adapters callable per lane. | Better robustness, lower correlated errors. | ~same | ~0% | P2 |
| RQ-26 (Retrieval mesh: multi-engine search) | feature | Single search stack can miss sources; overlaps with Gemini-default (shipped); extends to corroboration across engines. | Pluggable retrieval mesh: Gemini-grounded default + optional parallel corroboration engines (not failover). Dedupe/rank by source quality+recency; per-provider hit diagnostics. | Corroboration engines pluggable; dedup+rank verified. Per-provider hit counts in diagnostics. | Higher recall + better corroboration. | +6-18 | +30-90% | P2 |
| RQ-24 (Research memory with revalidation) | feature | Vetted prior evidence not reused; stale reuse risk unmanaged. | Memory store of vetted claims with decay + mandatory revalidation before reuse. | Claim-memory store operational; decay + revalidation gate verified on fixture. Reuse in new runs surfaces in diagnostics. | Faster future runs with controlled freshness risk. | 0-2 | 0-10% | P2 |
| MX-09 (Matrix cell jump-to-detail) | feature | Matrix grid cells have compact summaries but no affordance to jump to detailed debate thread. | Per-cell `open details` affordance deep-linking to matching item in Debate & Challenges (subject × attribute), focus/scroll targeting. | Click cell → Debate tab opens scrolled to that cell's thread. | Faster review on large matrices. | 0 | 0% | P2 |
| RA-12 (Confidence-weighted score) | improvement | Weighted score ignores confidence quality. | Add confidence-weighted score alongside raw (e.g. H=1.0, M=0.85, L=0.65); display both. Prerequisite: RQ-21. | Both scores visible in UI + export; weights documented. | Better ranking trust under uncertainty. | 0 | 0% | P2 |
| RA-16 (Dedicated discover prompt) | improvement | Discovery generation can be generic. | Add `SYS_DISCOVER` prompt optimized for strategic adjacent opportunities + clearer candidate rationale. | `SYS_DISCOVER` used for discovery phase; output quality measurably improved on 3-5 test cases. | Better discover candidates. | 0 | 0% | P2 |
| CF-02 (Polarity + research hints) | improvement | Many dimensions lack robust polarity/research hints. Prerequisite: ensure engine retrieval consumes these. | Add `polarityHint` + rich `researchHints` (`whereToLook`, `queryTemplates`) to all dimensions across configs. Verify retrieval pipeline reads them. | All dimensions carry hints. Retrieval stage logs reference hint usage. | Better targeted retrieval per research type. | 0 | 0% | P2 |
| CF-03 (Explicit outputMode) | fix | Scorecard configs can rely on default output-mode behavior. | Keep explicit `outputMode` in every config. | All configs have explicit `outputMode`. Test rejects configs without. | Stability and clarity. | 0 | 0% | P2 |
| CF-06 (Config schema refactor) | improvement | Config definitions harder to contribute than necessary. | Self-contained per-config objects + shared global defaults with lightweight normalization. | New config addable without touching shared defaults unless explicit override needed. Docs updated. | Easier contribution, lower drift risk. | 0 | 0% | P2 |
| DM-01 (Demo research pack) | feature | New users start empty; cannot benchmark output shape quickly. | Ship static demo pack fixtures (scorecard + matrix); tag as demo; representative high-quality examples. | Demo pack visible on empty-state; opens representative runs; labeled as demo. | Faster onboarding + quality calibration. | 0 runtime | 0% | P2 |
| UX-07 (Clarifying questions in Research Setup) | feature | Broad/ambiguous objectives produce weaker downstream research. Pre-research clarification lifts accuracy. | Extend `Research Setup` modal (`app/src/App.jsx` — `showSetupModal` + `setupDraft`) with "Sharpen Scope" step: after initial draft, Analyst call proposes 2-4 clarifying questions (geography, B2B/B2C, decision horizon). User answers persist as structured fields. Optional/skippable. No new pipeline stage — pre-pipeline UI. | Setup modal offers Sharpen step; questions load; answers persist into setup draft; integration tests cover skip + answer paths. | Improves decision-grade quality at the source. | +1 pre-run (small) | +2-5% | P2 |
| CONF-01 (Deterministic confidence derivation) | architecture | Confidence is free-form model self-report. Even after SQ-08 numeric fix, model is asked to rate its own certainty — LLMs poorly calibrated. Gate thresholds assume confidence is a signal; today it's noise smoothed by convention. | Replace (or supplement) model-expressed confidence with deterministic function of evidence signals: count of `groundedByProvider: true` sources, independent source count, verified-tier ratio (SQ-12), source-recency median, corroboration count, critic factual flags resolved/unresolved. Function: `confidence = f(grounded, independent, verifiedRatio, recencyScore, corroboration, criticFactualUnresolved)`. Parameters tuned against FR-02 benchmark. Emit both `confidence` (deterministic) and `confidenceSelfReported` (model's). `confidenceSource: derived | model_fallback`. Fallback to self-report when retrieval signals too thin. Confidence-nudge pathway for niche-but-correct single-source claims (bounded, audited). | On FR-02 benchmark, deterministic confidence matches labeled ground truth ≥80% of cells vs self-report ~50-60%. Gate thresholds tune against stable signal. Debug bundle shows both values side-by-side. | Depends on RETR-01 (deterministic corpus) + FR-02 (benchmark for tuning). Decision gate becomes trustworthy; prerequisite for decision-grade claims vs mainstream deep-research. | 0 | 0% | P3 |
| CO-01 (Share links, TTL, access controls) | feature | Shared research needed for async review; current flow lacks controlled link-based access + expiry. | Share links for one or more researches, configurable TTL, revoke controls, "shared with me" access model. | Link created, TTL enforced, revoke works, recipient without account can view (read-only). Blocked on DA-05 + UA-01. | Lightweight collaboration without forcing full account for reviewers. | 0 | 0% | P3 |
| CO-02 (Collaborative comment threads) | feature | Challenge threads single-user; reviewer feedback cannot be captured inline. | Per-dimension/per-cell comments + replies with participant labeling, notifications, owner moderation/revoke. | Comments threadable, labeled, notifiable, moderatable. Blocked on CO-01. | Turns outputs into collaborative decision artifacts. | 0 | 0% | P3 |
| FR-02 (Benchmark regression suite) | feature | Pipeline/prompt/model changes can silently degrade quality; no way to measure impact. | Hybrid benchmark stack: (1) replay regression tests in CI using dumped model/tool responses (zero API spend), (2) small live canary subset on schedule to detect provider/retrieval drift, (3) full live benchmark pre-release + before major quality claims. Start 6-8 gold cases, expand to 20. Fixed scoring rubric + scorer protocol for reproducibility. Prerequisite for DA-04 and CONF-01 tuning. | Replay suite runs in CI with zero API spend. Canary runs on schedule. Rubric + scorer protocol documented. Gold case count ≥ 6. | Measurement infrastructure; prevents regressions; makes "beats mainstream" claims auditable. | low recurring + periodic full run | low recurring + periodic | P3 |
| DA-04 (External Deep Research head-to-head harness) | feature | "Beats mainstream deep research" cannot be claimed without side-by-side evidence. | Benchmark harness running fixed prompts against Researchit, ChatGPT Deep Research, Claude Research, Gemini Research; score with rubric (accuracy, source quality, decision utility, contradiction handling); publish win/loss deltas. Depends on FR-02. | Harness runs all four systems on fixed prompts; rubric-scored; win/loss published in debug artifacts. | Objective proof of quality direction. | benchmark-only | benchmark-only | P3 |
| DA-06 (Deep Research ×3 run manifest + evidence cache) | feature | Retries/re-runs re-pay for identical deep calls; audits hard. | Persist deterministic run manifest (prompt hash + config version + provider/model set + timestamps); cache raw provider outputs + normalized extraction; reuse on safe retries/replays; expose provenance in debug/export. | Manifest deterministic; cache hit reduces replay cost; provenance visible. | Lower avoidable cost, reproducibility, auditability. Overlaps with RES-01a (output cache) and RES-02 (raw cache) at x3-specific grain — coordinate key schema to avoid duplication. | 0 happy path | 0% to lower | P3 |
| RQ-07 (Deep mode: triple baseline) | feature | Standard mode lacks explicit model-divergence signal. | Optional deep mode with parallel baseline passes + disagreement-guided retrieval focus. Depends on RQ-25. | Deep mode runs parallel baselines; disagreement surfaces; targeted retrieval increases recall on disputed cells. | Better calibration on high-stakes runs. | +3 parallel | +15% | P3 |
| RQ-08 (Targeted multi-model search) | feature | Single-model query framing misses retrieval diversity in disputed cells. | In deep mode, run disputed-dimension retrieval through multiple models, merge evidence, reconcile. Depends on RQ-25 + RQ-07. | Disputed-cell retrieval runs through multi-model; merge+reconcile validated on fixture. | Higher recall in contentious cells. | +4-8 | +20-40% | P3 |
| RQ-19 (Evidence graph layer) | feature | Claims stored as prose; provenance + contradiction tracking hard. | Structured claim graph (`claim → evidence nodes → source spans`) with recency/verification/conflict metadata. Score from graph signals, not only narrative. | Graph constructed per run; scoring reads graph; contradiction detection visible. Prereq: SQ-12 fabrication signal landed (Tier 1) so graph isn't built on broken confidence. | Strong auditability; machine-checkable logic. | 0-2 | 0-10% | P3 |
| ENG-05 (Align `analysisMeta` initialization) | refactor | `analysisMeta` initialized in three places (App.jsx partial, engine `createInitialState` full, `runMatrixAnalysis` partial re-spread) with different field sets. Stale values from one layer can leak. | Centralize `analysisMeta` init in engine only. App.jsx should not pre-build — engine owns the full shape. | App.jsx doesn't build `analysisMeta`; engine authoritative; fixture verifies shape consistency. | Prevents subtle field mismatch bugs as diagnostics expand. | 0 | 0% | P3 |
| ENG-08 (Shared source universe normalization) | refactor | `normalizeSourceUniverse`/`cleanText` logic repeated in `SourcesList.jsx`, `DimensionsTab.jsx`, `export.js` → formatting drift risk. | Shared frontend utility `app/src/lib/sourceUniverse.js`; reuse in UI + export. | Three consumers import the shared util; formatting identical on fixture. | Consistent source-universe presentation; fewer UI/export mismatches. | 0 | 0% | P3 |
| ENG-15 (Dedupe source-type inference) | refactor | `inferSourceType` exists independently in `06-source-verify.js` and `decision-gate.js`. Host lists drift. | Extract to `engine/lib/sources/source-type.js`; import in both. Single host list. Keep test suite covering both consumers. | Both consumers import shared module; host list in one place; tests cover both call sites. | Prevents silent drift. | 0 | 0% | P3 |

---

### Integration Acceptance (post-Tier-1.5)

After Tier 1.5 lands, re-run the 9×8 US-healthcare enterprise-products matrix. The matrix must pass the decision gate with all of the following observable:
- Zero `grounding_extraction_failed`, `stage_03b_no_search_performed`, `prompt_compaction_applied` (on stage 14), `plan_subject_count_mismatch`, `defend_missing_response`, or `source_missing_verification_tier` reason codes on the final successful run.
- `fabricationRatio` in decision-gate summary reflects real fabrication only (not missing grounding); `unknownRatio` surfaced separately.
- Every stage 03b chunk has `webSearchCalls > 0` AND `groundedSourceCount > 0`.
- Stage 08 recovery sources all carry `verificationTier`; zero legacy `notFound` fallback hits in gate.
- Stage 14 synthesis prompt fits without compaction; output references every subject.
- Stage 02 `plannerReturnedUnits` matches expected count.
- PAR-01: stage 03a ≥40% faster, stage 03b ≥35% faster vs pre-parallel baseline; cellsByKey bit-identical to serial fixture.
- RES-02: raw provider responses for stages 03a/03b/08 retrievable via API; `reprocessStage` re-normalizes with zero new provider calls.
- Tier 1 effects re-verifiable: `confidence_scale_coerced` count reported, `fabricationSignal` distribution correct per cell, gate gates on `maxFabricatedSourceRatio`.

### Integration Acceptance (post-Tier-1.6)

After Tier 1.6 lands, re-run the same 9×8 matrix. Additional requirements on top of Tier 1.5 acceptance:
- **GATE-02**: grep for `fabricationSignal` in debug bundle finds ≥1 hit per run. All decision-gate fields (`fabricationSignal`, `fabricationSignalReason`, `fabricationRatio`, `unknownRatio`, `unverifiableRatio`, `verifiedRatio`) present at documented key paths. `decisionGateEmissionCheck: true` on stage 15.
- **GND-01**: zero discrepancies between stage-level `meta.groundedSources` count and cell-level `groundedByProvider: true` count (±5% tolerance). `groundingPropagation` diagnostic present on stages 03a/03b/08. Memory-path (03a) sources show `groundingConfidence: "memory"`.
- **FAB-01**: stage 06 `fabricated` tier ≤10% of source population (was 75% in T12-46). `unverifiableRatio` surfaced in gate summary and is NOT gate-failing. Gate `fabricationRatio` computed over correctly-classified fabricated tier only.
- **GND-02**: stage 08 recovery `fabricated` tier ratio ≤15% (was 97% in T12-46). `stage08Recovery` diagnostic present.
- **RETR-01**: zero `source_absent_from_corpus` reason codes after retrieve/read split (i.e., read sub-stage never emits URLs outside corpus). Retrieve-call and read-call counts emitted separately per chunk. Fabricated-tier count on 03b ≤5%. Stage 08 same. Stage 14 synthesis references only corpus URLs.
- quality-bar.md "Maximum Extraction, Not Minimum Filtering" principle holds: the gate does not fail when the pipeline operates correctly; gate thresholds calibrated against what memory + retrieval realistically produce on well-documented topics.

Individual task acceptance verifies mechanisms; passing this integration run verifies the combined effect.

---
