{
  "name": "researchit-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd app && npm run build",
    "test:engine": "cd engine && npm run test",
    "fetch-raw": "node scripts/fetch-raw.mjs"
  }
}
